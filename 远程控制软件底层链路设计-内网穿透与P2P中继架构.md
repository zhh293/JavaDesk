# 远程控制软件底层链路设计：内网穿透与 P2P 中继架构

> 核心理念：**P2P 优先，智能中继兜底**  
> 适用场景：类似 ToDesk / 向日葵 / RustDesk 等远程桌面/远程控制软件  
> 文档定位：从计算机网络底层原理出发，逐环节讲透 NAT 穿透、打洞失败机理与完整链路设计

---

## 目录

- [第一部分：NAT 与内网穿透原理](#第一部分nat-与内网穿透原理)
  - [1.1 NAT 的本质](#11-nat-的本质)
  - [1.2 NAT 四大类型详解](#12-nat-四大类型详解)
  - [1.3 STUN 协议：让终端发现自己的公网映射](#13-stun-协议让终端发现自己的公网映射)
  - [1.4 ICE 框架：候选收集 → 连通性检查 → 选路](#14-ice-框架候选收集--连通性检查--选路)
  - [1.5 UDP 打洞的完整时序过程](#15-udp-打洞的完整时序过程)
  - [1.6 打洞成功的必要条件](#16-打洞成功的必要条件)
  - [1.7 打洞失败的典型场景](#17-打洞失败的典型场景)
  - [1.8 NAT 保活：为什么重要、怎么保活](#18-nat-保活为什么重要怎么保活)
- [第二部分：代理/路由导致打洞失败的深层机理](#第二部分代理路由导致打洞失败的深层机理)
  - [2.1 为什么路由器上的代理插件会让打洞失败、保活映射失效](#21-为什么路由器上的代理插件会让打洞失败保活映射失效)
  - [2.2 根因分类总览](#22-根因分类总览)
  - [2.3 典型故障路径详解](#23-典型故障路径详解)
  - [2.4 现象与抓包特征](#24-现象与抓包特征)
  - [2.5 检测与降级策略](#25-检测与降级策略)
  - [2.6 工程规避建议](#26-工程规避建议)
- [第三部分：P2P 优先 + 智能中继的底层链路设计](#第三部分p2p-优先--智能中继的底层链路设计)
  - [3.1 总体架构：控制面与数据面分离](#31-总体架构控制面与数据面分离)
  - [3.2 端到端会话时序](#32-端到端会话时序)
  - [3.3 会话状态机](#33-会话状态机)
  - [3.4 候选路径收集与链路探测](#34-候选路径收集与链路探测)
  - [3.5 传输与通道设计](#35-传输与通道设计)
  - [3.6 降级与恢复机制](#36-降级与恢复机制)
  - [3.7 QoS 与拥塞控制](#37-qos-与拥塞控制)
  - [3.8 加密与密钥协商](#38-加密与密钥协商)
  - [3.9 断线重连与路径迁移](#39-断线重连与路径迁移)
  - [3.10 TURN / 自建中继职责边界](#310-turn--自建中继职责边界)
  - [3.11 日志、监控与可观测性](#311-日志监控与可观测性)
  - [3.12 各阶段失败码与原因](#312-各阶段失败码与原因)
  - [3.13 工程参数设计：超时、重试、降级门限](#313-工程参数设计超时重试降级门限)
  - [3.14 实施路线图](#314-实施路线图)

---

# 第一部分：NAT 与内网穿透原理

## 1.1 NAT 的本质

NAT（Network Address Translation，网络地址转换）是 IPv4 地址耗尽背景下的必然产物。它的核心机制是：

- **五元组映射**：NAT 设备按照五元组（源 IP、源端口、目的 IP、目的端口、协议号）维护一张映射表，将内网 `私网IP:端口` 改写为公网 `公网IP:端口`。
- **单向可见**：内网主机可以主动向外发包（出站），NAT 会建立映射并放行回包；但外部主机无法主动向内网发起连接（入站），因为映射尚未建立。
- **UDP 无连接**：TCP 有三次握手，NAT 可以可靠地跟踪连接状态；UDP 无连接概念，NAT 只能依赖"最近有出站流量"来判断映射是否有效，因此 **UDP 映射有超时时间**（通常几十秒到几分钟不等）。

这就是为什么内网中的两台设备无法直接通信的根本原因——彼此的入站方向都被各自的 NAT 阻挡。

## 1.2 NAT 四大类型详解

RFC 3489 定义了经典的 NAT 四分类，工程上更推荐用"映射行为 + 过滤行为"（RFC 4787/5382）来描述，比四分类更精确，但四分类仍然是最直观的理解方式：

### 全锥形 NAT（Full Cone NAT）

```
内网 10.0.0.5:40000  →  NAT 映射为  公网 1.2.3.4:50000

映射规则：一旦内网某端口建立了映射，任意外部 IP:port 都可以向公网 1.2.3.4:50000 发包，
         NAT 会将包转发到 10.0.0.5:40000。
```

- **映射行为**：端点无关映射（Endpoint-Independent Mapping）——同一内网源端口访问不同外部目的时，公网映射不变。
- **过滤行为**：端点无关过滤（Endpoint-Independent Filtering）——任意外部地址都可以回包。
- **打洞难度**：★★☆☆☆ 最容易。只要一方向外发包建立映射，对端就可以直接发包进入。

### 受限锥形 NAT（Restricted Cone NAT）

```
内网 10.0.0.5:40000  →  NAT 映射为  公网 1.2.3.4:50000

过滤规则：仅允许"内网主机曾经发过包的外部 IP"回包（端口可不一致）。
         即：如果内网主机曾向 5.6.7.8 发过包，则 5.6.7.8 的任意端口都可以回包。
```

- **映射行为**：端点无关映射。
- **过滤行为**：地址依赖过滤（Address-Dependent Filtering）。
- **打洞难度**：★★★☆☆ 需要双方都先向对方发包，使得各自 NAT 认可对方的 IP。

### 端口受限锥形 NAT（Port Restricted Cone NAT）

```
内网 10.0.0.5:40000  →  NAT 映射为  公网 1.2.3.4:50000

过滤规则：仅允许"内网主机曾经发过包的外部 IP+端口"回包。
         即：如果内网主机曾向 5.6.7.8:60000 发过包，则只有 5.6.7.8:60000 可以回包。
```

- **映射行为**：端点无关映射。
- **过滤行为**：地址和端口依赖过滤（Address and Port-Dependent Filtering）。
- **打洞难度**：★★★★☆ 这是家庭路由器最常见的行为。双方必须几乎同时向对方的公网映射地址发包，才能让各自 NAT 放行回包。

### 对称 NAT（Symmetric NAT）

```
内网 10.0.0.5:40000  →  访问 STUN 5.6.7.8:3478  →  NAT 映射为 1.2.3.4:50000
内网 10.0.0.5:40000  →  访问对端 9.10.11.12:8000 →  NAT 映射为 1.2.3.4:62001

映射规则：同一内网源端口访问不同外部目的时，NAT 会分配不同的公网端口。
         对 STUN 服务器看到的映射 ≠ 对真实对端通信时的映射。
```

- **映射行为**：端点依赖映射（Endpoint-Dependent Mapping）——映射随目的地址变化。
- **过滤行为**：通常也是地址和端口依赖过滤。
- **打洞难度**：★★★★★ 最难。对端从信令中拿到的公网映射地址（从 STUN 获得的）与实际通信时的映射地址不同，导致对端发包到达 NAT 时端口不匹配，包被丢弃。

> **关键理解**：打洞能否成功，取决于双方 NAT 的"映射行为 + 过滤行为"组合。经典结论：  
> - 锥形 + 锥形 → 可打洞  
> - 锥形 + 对称 → 可能打洞（需要端口预测技巧，成功率低）  
> - 对称 + 对称 → 基本不可能打洞

## 1.3 STUN 协议：让终端发现自己的公网映射

STUN（Session Traversal Utilities for NAT）是一个轻量级协议，核心作用只有一个：**让内网终端发现自己的公网映射地址**。

### 工作流程

```
内网终端 10.0.0.5:40000
        │
        │  1. Binding Request（UDP）
        ▼
  STUN 服务器 5.6.7.8:3478
        │
        │  2. Binding Response
        │     XOR-MAPPED-ADDRESS = 1.2.3.4:50000
        ▼
内网终端获知：我在公网侧的映射是 1.2.3.4:50000
```

### STUN 做了什么

1. 终端向 STUN 服务器发送 `Binding Request`（UDP 包）。
2. STUN 服务器收到包后，看到的是 NAT 改写后的公网源地址 `1.2.3.4:50000`。
3. STUN 服务器在 `Binding Response` 中把这个地址回传给终端。
4. 终端现在知道了自己的公网映射地址，可以将其作为候选地址（`srflx candidate`）通过信令告知对端。

### STUN 做不到什么

- STUN **不能穿透 NAT**——它只是"发现"映射地址，实际穿透需要打洞或中继。
- STUN **不能保证映射地址始终有效**——映射会在 UDP 空闲超时后失效。
- STUN **不能发现对称 NAT 的真实映射**——对 STUN 服务器看到的端口 ≠ 对对端通信时的端口。

### NAT 行为检测（RFC 5780 思路）

使用多个 STUN 服务器或变更地址测试，可以推断 NAT 类型：

1. 向 STUN Server A 发包，获得映射地址 M1。
2. 向 STUN Server B 发包，获得映射地址 M2。
3. 如果 M1 == M2（IP 和端口都相同），则为端点无关映射（锥形 NAT）。
4. 如果 M1 ≠ M2，则为端点依赖映射（对称 NAT）。
5. 进一步测试过滤行为：让 STUN 服务器从不同 IP/端口 回包，观察是否通过。

## 1.4 ICE 框架：候选收集 → 连通性检查 → 选路

ICE（Interactive Connectivity Establishment）不是一个单一协议，而是一套完整的流程框架：

### 三种候选地址

| 候选类型 | 英文 | 来源 | 示例 | 优先级 |
|---------|------|------|------|--------|
| 主机候选 | Host Candidate | 本地网卡 | `192.168.1.5:40000` | 最高 |
| 服务器反射候选 | Server Reflexive (srflx) | STUN 返回 | `1.2.3.4:50000` | 中 |
| 中继候选 | Relay Candidate | TURN 分配 | `turn-server:60000` | 最低 |

### ICE 完整流程

```
┌─────────────────────────────────────────────────┐
│  1. 候选收集 (Candidate Gathering)               │
│     - 收集 host / srflx / relay 候选              │
│     - 支持 trickle ICE（逐步上报，不等全部完成）   │
├─────────────────────────────────────────────────┤
│  2. 候选交换 (Candidate Exchange)                │
│     - 通过信令通道互换候选列表 + ICE 凭据          │
│     - 包含优先级、用户名片段、密码等               │
├─────────────────────────────────────────────────┤
│  3. 连通性检查 (Connectivity Check)              │
│     - 对所有候选对按优先级并发做 STUN 检查          │
│     - 检查报文本身也在"制造/刷新 NAT 映射状态"     │
├─────────────────────────────────────────────────┤
│  4. 候选提名 (Nomination)                        │
│     - 控制方（controlling）提名最优候选对           │
│     - 双方锁定传输路径                             │
├─────────────────────────────────────────────────┤
│  5. 业务数据传输                                  │
│     - 在选定路径上传输远控数据                      │
│     - 持续保活 + consent freshness 检查            │
└─────────────────────────────────────────────────┘
```

### ICE 关键设计点

- **同一 UDP Socket**：候选收集与业务传输应复用同一个 UDP socket，否则 NAT 会为不同 socket 分配不同映射，导致地址不一致。
- **Trickle ICE**：不必等所有候选收集完成才开始交换，可以先发首批候选，后续逐步补充，减少建链时延。
- **角色分配**：ICE 定义了 `controlling` 和 `controlled` 两个角色，由控制方决定最终提名的候选对。

## 1.5 UDP 打洞的完整时序过程

以双方都在端口受限锥形 NAT 后为例（最常见的家庭网络场景）：

```
时间轴 →

被控端（Agent）                              控制端（Controller）
内网: 10.0.0.5:40000                        内网: 192.168.1.5:50000
公网: 1.2.3.4:60000                         公网: 5.6.7.8:70000
     │                                           │
     │  ① 双方分别向 STUN 发 Binding Request      │
     │     获得各自公网映射                        │
     │                                           │
     │  ② 通过信令交换公网映射地址                  │
     │     Agent 告知 Controller: 我的公网是 1.2.3.4:60000
     │     Controller 告知 Agent: 我的公网是 5.6.7.8:70000
     │                                           │
     │  ③ 双方几乎同时向对方公网地址发 UDP 包        │
     │                                           │
     ├─── UDP ──→ 5.6.7.8:70000 ─────────────────┤
     │    (Agent 的 NAT 建立/刷新映射)              │
     │    (到达 Controller 的 NAT 时，              │
     │     Controller 之前也向 Agent 发过包，        │
     │     所以 Controller 的 NAT 放行此包)          │
     │                                           │
     ├─── ←──────────── 5.6.7.8:70000 ── UDP ────┤
     │    (Controller 的 NAT 也已放行 Agent 的回包) │
     │                                           │
     │  ④ 双向通路建立，开始业务数据传输              │
```

### 为什么常说双方要"几乎同时"发包

这里更准确的说法不是"必须严格同时"，而是：**在大多数真实 NAT 场景下，双方需要在较短时间窗口内都向对方发包。**

原因是打洞不只是看"最外层公网端口有没有映射"，而是要看**整条回包路径上的每一层 NAT/过滤状态**是不是都允许这个包通过。

#### 先说一个容易误解的点

很多人会觉得：

- 我已经通过 STUN 拿到了自己的公网 `IP:port`；
- 如果运营商最外层 NAT 已经把这个端口映射出来了；
- 那对端只要对着这个端口发包，不就应该能一路打回来了吗？

这个理解只在一个比较理想的前提下成立：**链路上的每一层 NAT 都是全锥形，而且映射稳定不过期。**

如果真的满足这个前提，那么确实不一定要求双方都发包。对端只要拿到你的公网 `IP:port`，直接回打就可能成功。

#### 为什么现实里通常不能只看运营商最外层 NAT

真实路径往往是这样的：

```text
你的设备 -> 家用路由器 NAT -> 运营商 CGNAT -> 公网
```

对端的包要打回来，必须依次通过：

1. 运营商 CGNAT；
2. 你家路由器 NAT；
3. 最后才能到你的设备。

所以不是"最外层开了洞就够了"，而是**每一层都要允许这个包通过**。

#### 一个最典型的反例

假设：

- 运营商 CGNAT 是全锥形；
- 你家路由器却是端口受限锥形 NAT。

此时你先对 STUN 发包，只能保证：

- 运营商那层知道这个公网端口该转给你家路由器；
- 你家路由器知道你之前访问过 STUN 服务器。

但如果对端此时直接发包回来，到了你家路由器这一层，路由器会检查：

- 这个外部来源 IP:port，我之前见过吗？
- 我是否已经向这个对端发过包？

如果答案是否定的，那么即使运营商 NAT 已经放行，**你家路由器这一层依然可能把包丢掉**。

这就是为什么打洞不能只看最外层 NAT，而要看整条链路上每一层 NAT 的映射和过滤行为。

#### "几乎同时"真正想表达的是什么

所谓"几乎同时"，不是要求双方在同一毫秒发包，而是说：

- A 要在较短时间窗内向 B 发包；
- B 也要在较短时间窗内向 A 发包；
- 这样双方各自的 NAT 才会都建立起"面向对方"的状态。

对于端口受限锥形 NAT 来说，这个状态往往包括两部分：

1. **映射状态**：内网端口当前映射成哪个公网端口；
2. **过滤状态**：是否允许这个外部 IP:port 的包进入。

A 向 B 发过包，A 的 NAT 才更可能允许来自 B 的回包；  
B 向 A 发过包，B 的 NAT 才更可能允许来自 A 的回包。

所以在大多数真实环境里，双方在短时间窗内互相发包，成功率最高。

#### 还有一个关键点：对 STUN 的映射，不等于对真实对端的映射

有些 NAT，尤其是目标相关映射或对称 NAT，会出现：

```text
对 STUN 发包  -> 公网映射 A:50000
对真实对端发包 -> 公网映射 A:62000
```

这意味着你先向 STUN 发包建立起来的，只是"到 STUN 的那条映射"。  
它并不自动等价于"对真实对端也能用同一个公网端口"。

所以双方互相朝对方发包，本质上也是在建立：

- A 到 B 的真实映射；
- B 到 A 的真实映射。

这一步在对称 NAT、多层 NAT、运营商大内网、复杂代理/策略路由场景下尤其关键。

#### 最后的准确结论

- **如果链路上的 NAT 都是全锥形，且映射稳定，那么未必要求双方都发包。**
- **但在大多数真实网络里，NAT 往往不是全锥形，而且经常是多层 NAT。**
- **因此工程上通常要求双方在较短时间窗内都向对方发包，以建立和刷新面向对方的映射与过滤状态。**

所以更严谨的说法应该是：**不是"必须严格同时"，而是"在大多数实际 NAT 场景下，需要双方在较短时间窗内都向对方发包"。**

#### 一个完整走查案例：把前面所有点串起来看

下面给一个尽量接近真实家庭网络的例子，把“多层 NAT、运营商大内网、过滤行为、为什么不能只看最外层、为什么双方通常都要发包”一次性串起来。

##### 网络拓扑

```text
Agent 设备
192.168.10.23:40000
    |
    | 家庭路由器 NAT（端口受限锥形 NAT）
    | 内网口: 192.168.10.1
    | 外网口: 100.72.16.8:53000
    v
运营商 CGNAT（全锥形 NAT，假设比较宽松）
公网映射: 42.18.9.100:62000
    |
    v
公网
    |
    v
Controller 所在网络
203.0.113.9:41000
```

这里故意设成一个很有代表性的组合：

- **最外层运营商 CGNAT 比较宽松**，甚至假设它是全锥形；
- **家用路由器更严格**，是端口受限锥形 NAT；
- 这样就能清楚看到：**最外层放行，不代表整条路径都能通。**

##### 角色说明

- `Agent`：被控端，在家里电脑上，内网地址是 `192.168.10.23:40000`
- `家庭路由器 NAT`：第一层 NAT
- `运营商 CGNAT`：第二层 NAT，也是用户真正暴露到公网的出口
- `Controller`：控制端，公网地址是 `203.0.113.9:41000`
- `STUN`：公网 STUN 服务器，地址是 `1.1.1.1:3478`

##### 第一步：Agent 先去问 STUN“我现在在公网看起来是谁”

Agent 发出一个 UDP 包：

```text
源: 192.168.10.23:40000
目的: 1.1.1.1:3478
```

经过第一层家庭路由器 NAT 后，变成：

```text
源: 100.72.16.8:53000
目的: 1.1.1.1:3478
```

再经过第二层运营商 CGNAT 后，变成：

```text
源: 42.18.9.100:62000
目的: 1.1.1.1:3478
```

于是 STUN 看到的就是：

```text
42.18.9.100:62000
```

STUN 回给 Agent：

> 你在公网侧的映射是 `42.18.9.100:62000`

这时 Agent 会把这个地址通过信令告诉 Controller。

##### 第二步：很多人会在这里产生一个直觉

看到这里，很容易得出一个直觉：

- 既然 `42.18.9.100:62000` 已经打开了；
- 那 Controller 直接对着这个地址发包，不就能打到 Agent 了吗？

如果**整条链路上的每一层 NAT 都是全锥形**，这个直觉是可能成立的。  
但我们这个例子里，只有运营商 CGNAT 假设成了全锥形，家里的路由器并不是。

##### 第三步：Controller 直接回打，会发生什么

现在 Controller 直接发包到 Agent 的 STUN 地址：

```text
源: 203.0.113.9:41000
目的: 42.18.9.100:62000
```

###### 在运营商 CGNAT 这一层

因为我们假设运营商 CGNAT 是全锥形，而且已经有过这条映射：

```text
42.18.9.100:62000 -> 100.72.16.8:53000
```

所以这一层会放行，并把包转给家用路由器：

```text
源: 203.0.113.9:41000
目的: 100.72.16.8:53000
```

到这里为止，你的直觉还是对的：

- **最外层 NAT 的确已经把包送回来了。**

###### 在家庭路由器 NAT 这一层

现在包到了第一层家庭路由器。问题来了：

家庭路由器之前只见过这样一条出站行为：

```text
192.168.10.23:40000  ->  1.1.1.1:3478
```

对于端口受限锥形 NAT 来说，它不只记住端口映射，还会记住：

- 我之前是朝哪个外部 `IP:port` 发出去的；
- 现在回来的这个包，是不是来自我见过的那个 `IP:port`。

但现在进来的包来源是：

```text
203.0.113.9:41000
```

这不是之前的：

```text
1.1.1.1:3478
```

于是家庭路由器会判断：

- 这个来源我没见过；
- 我还没有允许这个对端进来；
- 丢弃。

结果就是：

- 运营商 CGNAT 放行了；
- 家用路由器这一层没有放行；
- 包停在了家门口，没进到 Agent。

##### 第四步：这就解释了为什么“不能只看最外层 NAT”

这个例子说明了一个很关键的事实：

- **最外层公网映射存在，只说明最外层 NAT 知道该把包转回哪一跳；**
- **并不说明内层 NAT 一定允许这个包继续进来。**

所以打洞真正要解决的问题，不是单纯让最外层 NAT 有映射，  
而是让**整条链路上的每一层 NAT，都形成对这个真实对端的允许状态。**

##### 第五步：那为什么双方在短时间窗内都发包，成功率就高了

现在让 Agent 也对 Controller 发一个包：

```text
源: 192.168.10.23:40000
目的: 203.0.113.9:41000
```

这时候：

1. 家庭路由器会记录：
   - `192.168.10.23:40000` 现在访问过 `203.0.113.9:41000`
   - 所以来自 `203.0.113.9:41000` 的回包是允许的
2. 运营商 CGNAT 也会为这条路径刷新/建立对应映射
3. Controller 那一侧如果也有 NAT，也会建立“面向 Agent”的状态

此时 Controller 再往 `42.18.9.100:62000` 发包时：

- 运营商 CGNAT 放行；
- 家用路由器也会说：这个来源我见过，允许进来；
- 包最终到达 `192.168.10.23:40000`

这时候双向通路才真正打通。

##### 第六步：为什么这里说的是“较短时间窗”，不是“同一毫秒”

这里的重点不是双方在同一时刻按下发送键，而是：

- 两边 NAT 针对彼此建立的状态，要在同一个有效窗口里存在；
- 不要出现 A 这边刚建立，B 那边还没建立，或者其中一边状态已经超时；
- 所以工程上会让双方在短时间窗内并发探测，提高成功率。

也就是说：

- **不是数学意义上的严格同时；**
- **而是网络状态意义上的“窗口重叠”。**

##### 第七步：再往前一步，如果是对称 NAT，会更复杂

上面的例子里，Agent 对 STUN 的映射和对 Controller 的映射，我们默认还可以复用。  
但如果家庭路由器或运营商 CGNAT 是对称 NAT，那么可能出现：

```text
Agent -> STUN        -> 42.18.9.100:62000
Agent -> Controller  -> 42.18.9.100:64012
```

这意味着：

- STUN 告诉对端的是 `62000`；
- 但 Agent 真正朝 Controller 发包时，实际用的却是 `64012`；
- Controller 如果还按 `62000` 去打，就会继续失败。

这又进一步说明：

- 对 STUN 的映射，不一定等于对真实对端的映射；
- 只盯着 STUN 拿到的地址，是不够的；
- 为什么真实工程里要做候选探测、连通性检查、失败后快速降级中继。

##### 这个案例最终说明了什么

把这个例子串起来，最终要记住 5 句话：

1. **公网映射存在，不等于对端一定能打进来。**
2. **最外层 NAT 放行，不等于内层 NAT 也会放行。**
3. **打洞不仅依赖映射，还依赖过滤状态。**
4. **在大多数真实网络里，双方都需要在较短时间窗内向对方发包。**
5. **如果 NAT 是对称的、路径复杂或多层 NAT 太严格，就要尽快降级到中继。**

## 1.6 打洞成功的必要条件

| 条件 | 说明 |
|------|------|
| 至少一个候选对可达 | 常见是 `srflx-srflx` 可达；若直连不可达但 `relay-relay` 可达也算成功（中继模式） |
| NAT 行为对打洞友好 | 锥形 NAT、端点无关映射/过滤的网络成功率更高；双边都不是强对称时成功率明显提升 |
| 双方都能主动出站 UDP | 在大多数实际 NAT 场景下，双方都需要在较短时间窗内向对方发包，以建立面向对方的映射和过滤状态；若一侧只能被动接收，通常失败 |
| 时序窗口匹配 | 两侧检查/打洞包到达在 NAT 状态未超时窗口内；信令延迟不能过高 |
| UDP 未被封禁 | 路径上无设备阻断 UDP；DNS/STUN 可达 |
| 保活持续 | 建立映射后持续保活，否则空闲超时映射失效 |

## 1.7 打洞失败的典型场景

### 场景一：对称 NAT 组合

```
Agent: 对称 NAT  →  对 STUN 映射是 1.2.3.4:50000，对 Controller 映射是 1.2.3.4:62001
Controller: 端口受限锥形 NAT  →  按 STUN 结果发到 1.2.3.4:50000

→ 端口不匹配，Agent 的 NAT 丢弃包 → 打洞失败
```

### 场景二：多层 NAT / CGNAT

运营商级 NAT（CGNAT）在用户路由器之上又加了一层 NAT，映射行为更不可预测，超时更短，过滤更严格。

### 场景三：企业防火墙策略

企业出口安全设备常对 UDP 做限流、超短超时、或直接阻断未知 UDP 流量。

### 场景四：UDP 被禁

某些网络仅允许 TCP/443 出站，STUN 和打洞完全无法进行。

### 场景五：NAT 绑定过短且无保活

映射提前失效，表现为"刚连上很快断"或"空闲后单向通/全断"。

### 场景六：同网关回环（Hairpin NAT）不支持

双方经同一 NAT 且使用公网地址互访时，需要 NAT hairpin 能力；不支持则该场景失败。应优先使用内网 host 候选。

## 1.8 NAT 保活：为什么重要、怎么保活

### 为什么必须保活

- UDP 无连接，NAT 靠"最近流量"维护映射状态。
- 无保活 → 映射超时 → 公网映射失效 → 对端再发包到旧地址，无法到达。
- 表现：空闲一段时间后，远控画面冻结、操作无响应、单向或双向断开。

### 保活节奏建议

| 网络类型 | 建议保活间隔 | 说明 |
|---------|-------------|------|
| 家宽/公网 | 15~30s | 大多数家用路由器 UDP 超时 30~120s |
| 企业网 | 10~20s | 企业设备 UDP 超时可能更短 |
| 移动网络 (4G/5G) | 8~15s | 运营商 NAT 超时短，且无线电休眠更快 |
| CGNAT | 10~15s | 运营商级 NAT 超时通常更激进 |

### 保活代价

- **带宽**：每小时几十 KB 量级/会话，可以忽略。
- **电量**（移动端主要成本）：每次保活都会唤醒无线电，频率越高耗电越大。
- **建议**：自适应保活——按网络类型、空闲时长、历史超时统计动态调整间隔，并加随机抖动（±20%）避免大量设备同时保活形成"保活风暴"。

### 保活实现方式

- STUN Binding Request/Response（标准方式，也刷新 ICE consent）。
- 自定义 UDP 心跳包（更轻量，但需要协议层支持）。
- 在 QUIC 连接中使用 PING 帧（如果已在 QUIC 上建链）。

---

# 第二部分：代理/路由导致打洞失败的深层机理

> **核心论点**：STUN 保活得到的公网 `IP:port` 只是在 **某一时刻、某一条出站路径、针对某一目标** 成立的"临时映射快照"。在路由器开启代理、旁路由、透明代理、VPN、策略路由等场景下，这个映射会因为路径或状态变化而失效。

## 2.1 为什么路由器上的代理插件会让打洞失败、保活映射失效

### 先说结论

严格来说，失效的并不是"保活 IP"本身，而是 **STUN 获取到的公网 `IP:port` 映射，以及这条映射对应的 NAT/路由状态**。

UDP 打洞成立依赖两个前提：

1. **探测路径和真实业务路径必须一致**。
2. **你告诉对端的那个公网 `IP:port`，必须真的是外面能直接打回你这台设备的地址**。

而路由器上的代理插件、旁路由、透明代理、VPN、策略路由，经常会同时破坏这两个前提，所以会出现：

- STUN 看见的地址不是后续真实业务数据使用的地址；
- 你保活住的是一条旧映射、旁路映射、代理映射，或者只对 STUN 服务器成立的映射；
- 对端继续按旧地址或错误地址发包，最终打洞失败。

### 保活保的到底是什么

保活保的不是某个固定公网 IP，而是 **NAT 设备中的一条临时映射表项**。

对 UDP 来说，NAT 一般会记住类似这样的状态：

- 哪个内网地址和端口发起了出站包；
- 这个包是发往哪个外部目标；
- 当前被映射成了哪个公网端口；
- 这条映射多久没有新流量了。

所谓保活，本质就是定期发包，避免这条映射被 NAT 回收。  
所以一旦代理插件改动了：

- 出站路径；
- 出口 IP；
- 出口端口；
- 发包主体；
- 目标相关的 NAT 映射关系；

那你保活住的，可能就已经不是对端真正会使用的那条映射了。

### 代理插件最常见的四种破坏方式

#### 方式一：STUN 走代理，业务流不走代理，路径不一致

正常情况下应该是：

```text
终端 -> 本机路由/NAT -> 公网 -> STUN
终端 -> 本机路由/NAT -> 公网 -> 对端
```

这两条路径如果一致，那么 STUN 拿到的公网 `IP:port` 才有可能被真实业务复用。

但开了代理插件后，经常会变成：

```text
STUN:  终端 -> 代理插件/旁路由 -> WAN2/代理隧道 -> STUN
业务:  终端 -> 本机 NAT -> WAN1 -> 对端
```

此时 STUN 看到的是 `WAN2` 或代理出口身份，而真实业务包走的是 `WAN1`。  
结果就是：**STUN 探测出来的出口，不是后续 P2P 业务真正使用的出口**。

对端拿到错误的 `IP:port` 去打洞，自然失败。

#### 方式二：代理在“代你发包”，外网看到的是代理出口，不是终端真实可回打地址

很多路由器代理插件并不是简单转发原始 UDP 包，而是：

1. 先由终端把流量送到本地代理进程；
2. 代理进程重新创建自己的 socket；
3. 再由代理进程、隧道接口或远端代理节点把包发出去。

于是 STUN 看到的源地址，未必是"你的设备经 NAT 后的出口地址"，而可能是：

- 路由器代理进程的出口端口；
- TUN/TAP 虚拟接口对应的出口；
- VPN 隧道出口；
- 远端代理节点的公网出口。

这样一来，对端虽然拿到了一个看似合法的公网 `IP:port`，但它并不一定能直接回到你的终端。  
因为很多代理只擅长做**出站转发**，并不具备把一个来自外部陌生源的 UDP 包，再准确反向递送到内网终端的能力。

#### 方式三：代理插件改变了源端口，导致映射漂移

打洞依赖的不只是公网 IP，还依赖公网端口。  
而代理、透明代理、隧道转发，经常会导致：

- 终端本地用的是 `40000` 端口；
- 代理出去重新开了一个 socket；
- 对外表现为 `51000`；
- 过一会因为重连、回收或策略切换又变成 `62000`。

这就是端口漂移（port drift / port rebinding）。

对端在信令阶段记住的可能还是旧地址：

```text
A:51000
```

但你当前真实出口已经变成：

```text
A:62000
```

于是对端还在往 `51000` 发包，NAT 和代理状态都对不上，包自然被丢弃。

#### 方式四：你保活的是“到 STUN 的映射”，不是“到真实对端的映射”

这是最容易被误解的一点。

很多人以为：只要持续对 STUN 服务器发保活包，自己那个公网 `IP:port` 就能一直有效。  
但如果当前网络环境表现得像对称 NAT 或目标相关映射，那么实际情况是：

```text
对 STUN 发包  -> 公网映射 A:50000
对真实对端发包 -> 公网映射 A:62000
```

也就是说，**你保活住的只是“到 STUN 的映射”**，而不是“到对端的映射”。  
这在代理插件、策略路由、多出口、对称 NAT 组合场景里尤其常见。

所以即使你一直在对 STUN 发包，真实对端那条映射仍然可能已经超时、变更，或者根本没有按你预期建立成功。

### 为什么常表现为“刚刚还行，过一会就不行了”

因为代理插件常常还会带来动态变化：

- 节点自动切换；
- VPN 重连；
- 多 WAN 切换；
- 策略路由命中变化；
- conntrack 表项回收；
- 代理进程重启或重建 socket。

这些变化一发生，原先的出口身份和 NAT 表项就可能瞬间失效。此时：

- 对端还在按旧 candidate 发包；
- 本端已经换了新的出口地址或端口；
- 双方对同一个会话的路径认知不一致；
- 结果就是打洞突然失败、会话突然断流、空闲后无法恢复。

### 一句话总结

代理插件之所以会让打洞失败，不是因为它只“改了 IP”，而是因为它可能同时改了：

- 出站路径；
- 出口 IP；
- 出口端口；
- 发包主体；
- NAT 映射与目标地址的对应关系。

所以最终结果就是：**STUN 看到的地址不再等于真实可回打地址，而保活也可能保错了对象。**

## 2.2 根因分类总览

| 根因类别 | 机制 | 影响 |
|---------|------|------|
| conntrack/NAT 映射易失性 | UDP 无连接，映射依赖 conntrack 状态表；超时、表项回收、规则重载、设备重启都会让旧 `IP:port` 作废 | 映射消失，对端发包到旧地址无响应 |
| 出口身份变化 | 多 WAN、VPN 上下线、策略路由命中变化会让流量从另一个出口走，公网 IP 变化 | STUN 看到的 IP 与实际出口 IP 不一致 |
| 端口重写与漂移 | NAT 可能重新分配外部端口（高负载或冲突回收后），保活频率不够时旧端口失效 | 对端打到旧端口，NAT 丢弃 |
| 对称 NAT | 对不同目标地址分配不同外部端口；对 STUN 服务器看到的端口 ≠ 对对端通信时的端口 | 信令交换的映射地址不可用 |
| 代理代发/透明代理改写 | STUN 请求可能由代理节点代发，得到的是"代理出口映射"而非真实端到端可达映射 | 真实入站无法回到终端 |
| UDP idle timeout 过短 | 部分设备 UDP 超时仅十几秒到几十秒 | 保活间隔稍大就断映射 |
| 路由抖动/链路切换 | 探测与正式打洞不在同一路由，五元组不连续 | NAT 状态不匹配 |
| Hairpin/回流限制 | 同 NAT 内设备通过公网地址互访时路由器不支持 | 局域网内也失败 |
| ALG/安全设备干预 | 企业防火墙、IPS/IDS、UDP flood 防护、SIP ALG 等可能丢包、改包、限速 | 打洞包被拦截或篡改 |

## 2.3 典型故障路径详解

### 路径分裂型

```
正常流程：终端 → WAN1 → STUN → 获得 1.2.3.4:50000
代理开启后：终端 → 旁路由 → WAN2 → STUN → 获得 9.10.11.12:50000

但业务包可能因为策略路由走了 WAN1（1.2.3.4），对端仍在打 9.10.11.12 → 失败
```

**本质**：STUN 走的路径 ≠ 业务流走的路径，导致公网映射不一致。

### 对称 NAT 型

```
终端对 STUN(5.6.7.8:3478) → 映射为 1.2.3.4:50000
终端对对端(9.10.11.12:8000) → 映射为 1.2.3.4:62001

信令告知对端用 50000 打洞 → 实际应该打到 62001 → 端口不匹配 → 失败
```

### 超时失效型

```
1. 终端向 STUN 获取映射 1.2.3.4:50000 ✓
2. 空闲 60 秒（NAT UDP 超时 30 秒）
3. 映射已被 NAT 回收
4. 对端发包到 1.2.3.4:50000 → NAT 无此映射 → 丢弃 → 失败
```

### 代理代发型

```
终端 → 透明代理(192.168.1.1:7890) → 代理出口(P:port) → STUN
STUN 返回：你的公网映射是 P:port

但 P:port 是代理节点的出口，不是终端直连可达的。
对端发包到 P:port → 代理不知道如何转发到内网终端 → 失败
```

### 重连漂移型

```
1. 建立会话，映射为 1.2.3.4:50000 ✓
2. VPN 重连 / 链路抖动 → NAT 表重建
3. 新映射为 1.2.3.4:58000
4. 双方仍在使用旧候选 50000 → 失败
```

### 同网回流型

```
Agent(10.0.0.5) 和 Controller(10.0.0.8) 在同一 NAT 后
双方用公网候选 1.2.3.4:50000 / 1.2.3.4:60000 互打
NAT 不支持 UDP hairpin → 回流失败

正确做法：应优先使用 host 候选 10.0.0.5 / 10.0.0.8 直连
```

## 2.4 现象与抓包特征

| 故障类型 | 现象 | 抓包特征 |
|---------|------|---------|
| 路径分裂 | 信令交换成功，但 UDP 单向或双向无数据 | LAN 侧看到发往代理/旁路由，WAN 侧出接口与 STUN 不一致 |
| 对称 NAT | 连通性检查全失败 | 同一内网源端口对不同目标，WAN 侧外部端口不同 |
| 超时失效 | 空闲后断开，重发无响应 | 空闲后首包触发新 NAT 映射，外部端口突变 |
| 代理代发 | STUN 结果与真实可达地址不匹配 | LAN 侧看到发往本地代理，WAN 侧只见代理节点对外发包 |
| 路由切换 | 偶发断连，恢复后可能正常 | 出接口、源公网 IP、TTL/时延模式发生突变 |
| 安全设备干预 | 周期性丢包、限速 | ICMP 不可达、固定大小包被异常丢弃、连接重置 |
| 回流失败 | 同网设备互连失败 | NAT 设备无正确 DNAT/SNAT 回写，LAN 无入站 |

## 2.5 检测与降级策略

### NAT 行为预检

- 使用多 STUN 服务器 / 变更地址测试（RFC 5780 思路）识别对称 NAT、端口依赖映射和过滤类型。
- 在会话建立前就推断打洞成功率，对低概率场景提前预留中继。

### 持续有效性验证

- 会话期定时 re-STUN + consent/probe。
- 检测到映射变更、连续丢失、RTT 异常即判定直连风险。

### 路径一致性检查

- 比较探测期与会话期的出口指纹（公网 IP、接口、路由表、VPN 状态）。
- 不一致立即重打洞或降级。

### 失败快速判定

- 在短窗口内（如若干个 RTT）未收到对端回包，且出现端口漂移/路径变化，直接切 TURN/中继。
- 不要让用户长时间"连接中"。

### 双路径并发策略

- 直连与中继并发建链（或中继热备）。
- 谁先稳定可用就切谁，避免长时间黑屏/卡住。

### 防抖与回切策略

- 降级后保持一段稳定期（如 30s）再尝试回切直连。
- 避免"直连-中继"频繁抖动导致体验劣化。

## 2.6 工程规避建议

| 建议 | 说明 |
|------|------|
| 中继是必要兜底，不是可选项 | 复杂网络下中继是可达性底线，永远不要假设打洞 100% 成功 |
| 缩短并自适应保活间隔 | 按网络类型动态调节 UDP keepalive，加随机抖动避免同步风暴 |
| 保证探测与业务同路径 | 同一 socket / 同一 fwmark / 同一策略路由，避免 STUN 与业务走不同出口 |
| 为 P2P 流量设置代理绕行 | 透明代理、旁路由规则中显式放行打洞端口和 STUN/TURN 目标 |
| 多候选与快速重协商 | 维护 host/srflx/relay 多候选，路径变化后快速 ICE restart/候选刷新 |
| 多 WAN/VPN 事件感知 | 监听链路和路由变化事件，触发重探测而不是等用户感知故障 |
| Hairpin 兼容策略 | 检测到同 NAT 场景时优先内网候选，避免走公网回流 |
| 可观测性建设 | 记录映射变化率、端口漂移率、直连成功率、降级原因码 |

> **一句话落地**：STUN 结果不是"永久地址"，而是"短时、路径相关的可达性快照"；工程上必须把"检测变化 + 快速重协商 + 中继兜底"作为默认设计。

---

# 第三部分：P2P 优先 + 智能中继的底层链路设计

## 3.1 总体架构：控制面与数据面分离

```
┌──────────────────────────────────────────────────────────────────┐
│                        控制面 (Control Plane)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ 鉴权服务  │  │ 信令服务  │  │ 会话编排  │  │ 策略/QoS 服务 │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ 在线状态  │  │ 审计日志  │  │ 告警/风控 │  │  可观测聚合   │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
│  传输协议：HTTPS / gRPC / WebSocket (TLS)                        │
│  特点：高可靠、低带宽、强一致                                      │
├──────────────────────────────────────────────────────────────────┤
│                        数据面 (Data Plane)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐    │
│  │ P2P 直连通道  │  │ TURN 中继通道 │  │ 自建中继 (Relay)   │    │
│  │ UDP/QUIC     │  │ UDP/TCP/TLS  │  │ QUIC/UDP/TCP      │    │
│  └──────────────┘  └──────────────┘  └────────────────────┘    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │           ICE Agent (候选管理 + 连通性检查 + 路径选择)      │   │
│  └──────────────────────────────────────────────────────────┘   │
│  传输协议：UDP/QUIC (首选) → TCP/TLS → WebSocket (兜底)         │
│  特点：高带宽、低时延、支持路径迁移                                │
├──────────────────────────────────────────────────────────────────┤
│                      业务层 (Application)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ 输入控制  │  │ 屏幕流    │  │ 文件传输  │  │ 音频/剪贴板   │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

### 为什么必须分离

- **扩展性**：控制面 QPS 高但带宽小；数据面带宽大且需边缘部署。分离后可独立扩缩容。
- **可靠性**：控制面抖动不应拖垮大流量转发；数据面故障不应影响鉴权与审计。
- **安全性**：控制面管理身份与权限，数据面最小化敏感元数据暴露。
- **运维性**：问题定位更清晰（是鉴权问题、信令问题还是链路质量问题）。

## 3.2 端到端会话时序

### 参与角色

| 角色 | 职责 |
|------|------|
| Controller（控制端） | 发起远控，发送键鼠/文件 |
| Agent（被控端） | 接收远控，推送屏幕/音频 |
| Signaling（信令服务） | 邀请、SDP/候选转发、状态同步 |
| Presence（在线状态服务） | 设备注册、心跳 |
| Session（会话编排服务） | 会话创建、权限、生命周期 |
| STUN/TURN | 候选发现与中继分配 |
| Relay（中继转发平面） | 兜底链路转发 |
| Policy/QoS | 策略与质量决策（降级、迁移、回切） |

### 详细时序

| 步骤 | 时序事件 | 关键输出 |
|------|---------|---------|
| 1 | `Controller → Presence` 设备上线鉴权，注册设备能力（网络类型、NAT 线索、支持协议、公钥指纹） | `onlineToken`、设备在线 |
| 2 | `Agent → Presence` 同步上线并定时心跳（如 15s） | 双端均可被路由 |
| 3 | `Controller → Session` 发起会话创建（目标设备、业务类型、权限凭证） | `sessionId`、会话票据 |
| 4 | `Session → Signaling → Agent` 下发来电邀请；`Agent` 接受/拒绝 | 接受后进入连接流程 |
| 5 | `Controller/Agent → STUN` 并行收集候选（`host/srflx/relay`），可启用 trickle 逐步上报 | 候选列表 + 优先级 |
| 6 | `Controller ↔ Signaling ↔ Agent` 交换候选、ICE 凭据、指纹 | 候选对构建完成 |
| 7 | 双端对候选对执行 UDP 打洞（并发发探测包） | NAT 映射建立 |
| 8 | 双端连通性检查（按优先级探测、统计 RTT/丢包）并 nomination 最优路径 | 选出首选链路 |
| 9 | **若 P2P 成功**：在选定路径上做安全握手（DTLS/QUIC TLS），派生会话密钥 | `P2PConnected` |
| 10 | 建立业务逻辑通道（输入控制、屏幕/音频流、文件、剪贴板、统计） | 可交互远控 |
| 11 | 会话期保活（传输层 ping + 控制面心跳），持续上报 QoS | 链路健康可观测 |
| 12 | 网络波动触发重协商（码率/分辨率/MTU）或候选增量交换 | 会话不中断调优 |
| 13 | 网络切换（Wi-Fi→4G）触发路径迁移：`make-before-break`，先建新路再切流 | 无感迁移 |
| 14 | **若 P2P 质量持续劣化或失败**：申请 `Relay`，复用会话上下文切到中继 | `RelayConnected` |
| 15 | 链路断开进入重连：优先恢复原 P2P，其次重打洞，最后中继兜底 | `Reconnecting` → Connected |
| 16 | 用户挂断/超时/策略终止：释放通道、回收票据、写审计日志 | `Ended` |

### 关键异常分支

- **被控端拒绝**：步骤 4 直接结束，状态转 `Ended`。
- **候选交换不完整**：步骤 6 超时后补发一次，仍失败则直降中继。
- **对称 NAT + UDP 受限**：步骤 7/8 多次失败后，跳过 P2P 继续尝试中继。
- **中继也不可用**：进入 `Reconnecting`，重试预算耗尽后 `Ended`。

## 3.3 会话状态机

```
                    ┌──────────────────────────┐
                    │         Idle             │
                    │  进程启动但未上线          │
                    └────────┬─────────────────┘
                             │ 注册成功
                             ▼
                    ┌──────────────────────────┐
                    │         Online           │
                    │  完成注册且心跳正常        │
                    └────────┬─────────────────┘
                             │ 发起/接收会话邀请
                             ▼
                    ┌──────────────────────────┐
                    │        Connecting        │
                    │  会话已创建、等待双方确认   │
                    └────────┬─────────────────┘
                             │ 双方确认，开始候选收集
                             ▼
                    ┌──────────────────────────┐
                    │        Probing           │
                    │  打洞、连通性检查、路径评分  │
                    └──┬──────────────┬────────┘
                       │ P2P 成功      │ 中继成功 / P2P 失败
                       ▼              ▼
          ┌──────────────────┐  ┌──────────────────┐
          │  P2PConnected    │  │ RelayConnected   │
          │  直连建链成功      │  │ 中继建链成功       │
          └──┬───────────────┘  └──┬───────────────┘
             │ 质量劣化              │ P2P 回切成功
             ▼                      ▼
          ┌──────────────────┐  ┌──────────────────┐
          │    Degraded      │  │  P2PConnected    │
          │  质量低于阈值      │  │  (回切)           │
          └──┬───────────────┘  └──────────────────┘
             │ 触发降级/断链
             ▼
          ┌──────────────────────────────────────────┐
          │            Reconnecting                  │
          │  指数退避重连、会话恢复、候选重采集         │
          └──┬─────────────┬────────────────────────┘
             │ 恢复成功      │ 预算耗尽
             ▼              ▼
    P2PConnected /     ┌──────────────────┐
    RelayConnected     │      Ended       │
                       │  释放资源、审计    │
                       └──────────────────┘
```

### 状态详解

| 状态 | 进入条件 | 核心动作 | 退出条件 | 目标状态 |
|------|---------|---------|---------|---------|
| `Idle` | 进程启动但未上线 | 初始化本地能力、证书、缓存 | 注册成功 | `Online` |
| `Online` | 完成注册且心跳正常 | 等待邀请/发起会话 | 发起或接收会话邀请 | `Connecting` |
| `Connecting` | 会话已创建、等待双方确认 | 邀请握手、权限校验、分配 `sessionId` | 双方确认且开始候选收集 | `Probing` |
| `Probing` | 候选交换完成 | 打洞、连通性检查、路径评分 | P2P 成功 / 中继成功 / 失败 | `P2PConnected` / `RelayConnected` / `Reconnecting` |
| `P2PConnected` | 直连建链成功 | 业务流传输、保活、质量监测 | 质量劣化/断链/挂断 | `Degraded` / `Reconnecting` / `Ended` |
| `RelayConnected` | 中继建链成功 | 中继传输、成本监测、后台回探 P2P | P2P 回切成功/断链/挂断 | `P2PConnected` / `Reconnecting` / `Ended` |
| `Degraded` | 已连接但质量低于阈值 | 降码率、降帧、小窗口探测备选路径 | 质量恢复/触发降级/断链 | `P2PConnected` / `RelayConnected` / `Reconnecting` |
| `Reconnecting` | 连接中断或握手失败 | 指数退避重连、会话恢复、候选重采集 | 恢复成功或预算耗尽 | `P2PConnected` / `RelayConnected` / `Ended` |
| `Ended` | 用户结束/超时/策略终止 | 回收资源、审计、上报指标 | 新会话触发 | `Idle` 或 `Online` |

## 3.4 候选路径收集与链路探测

### 候选收集

- **Host Candidate**：本地网卡地址（如 `192.168.1.5:40000`）。同一 NAT 后的设备应优先用此直连。
- **Server Reflexive (srflx)**：通过 STUN 获得的公网映射（如 `1.2.3.4:50000`）。
- **Relay Candidate**：通过 TURN 分配的中继地址（如 `turn-server:60000`）。

### 链路探测打分

对每个候选对进行并行探测，统计 RTT、抖动、丢包率、可用带宽，然后计算综合评分：

```
score = reachable × w0 − rtt × w1 − loss × w2 − jitter × w3 + bandwidth × w4 − relay_penalty
```

### 路径优先级

```
P2P-QUIC(UDP) > P2P-UDP(自定义) > Relay-QUIC(UDP) > Relay-TCP/TLS > WebSocket(443)
```

## 3.5 传输与通道设计

### 为什么首选 UDP/QUIC

| 优势 | 说明 |
|------|------|
| 低时延 | 无 TCP 队头阻塞，适合实时交互 |
| 多路复用 | 控制、视频、文件共用一条连接但互不阻塞 |
| 连接迁移 | IP 变化（Wi-Fi 切 4G）可保会话不中断 |
| 内建安全 | TLS 1.3 标配，减少自研加密复杂度 |
| 弱网表现 | 配合自适应码率与前向纠错更稳 |

### 通道划分

建议所有业务统一跑在 QUIC 连接内，按 stream/datagram 分通道：

| 通道 | 传输形态 | 可靠性 | 优先级 | 说明 |
|------|---------|--------|--------|------|
| 控制指令（键鼠） | QUIC stream | 可靠、低时延 | **P0 最高** | 小包，需抢占调度，永不排队 |
| 屏幕流 | QUIC datagram + 关键帧补偿 | 部分可靠 | P1 高 | 优先实时性，允许少量丢帧 |
| 音频流 | datagram | 部分可靠 | P1 高 | 低时延优先 |
| 剪贴板/控制消息 | stream | 可靠 | P2 中高 | 小体量控制数据 |
| 文件传输 | QUIC stream | 强可靠 | P3 中 | 大吞吐，背景带宽填充，避免抢占实时通道 |

**要点**：
- 屏幕流与文件流严格隔离，避免文件传输拖慢远控手感。
- 通过 stream 优先级/权重实现"键鼠包永不排队"。
- 文件传输可限速，给交互流保留最小带宽。

### 何时用 TCP/WebSocket 兜底

进入兜底的典型场景：
- 企业网/校园网封锁 UDP 或限 QUIC
- 双方 NAT 组合导致打洞不可达（尤其对称 NAT）
- 中间盒对 UDP 严格限流或 DPI 阻断
- 合规要求仅允许 443/TLS 出口

策略建议：不要"一刀切改 TCP"，保留"可回升"机制——网络恢复后可再尝试升级回 UDP/QUIC。

## 3.6 降级与恢复机制

### 降级阶梯

```
P2P-QUIC(UDP)
    │ 失败/劣化
    ▼
P2P-自定义UDP协议
    │ 失败/劣化
    ▼
Relay-QUIC(UDP中继)
    │ 失败
    ▼
Relay-TCP/TLS(中继)
    │ 失败
    ▼
WebSocket over 443
    │ 失败
    ▼
会话结束 (Ended)
```

### 降级触发条件

**硬门限（立即降级）**：
- 连通性检查全失败
- 连续 3 个保活周期无响应
- 传输层明确 close/reset

**软门限（持续劣化才降级）**：
- 丢包率 > 15% 持续 3s
- RTT > 350ms 持续 5s
- 卡顿率（冻结帧占比）> 20% 持续 3s

### 回切条件（Relay → P2P）

- 中继保持最短驻留 30s（防抖）
- 后台探测 P2P 连续成功 3 次且质量提升显著（如时延下降 > 20%）
- 才回切到 P2P

### 设计要求

- **降级自动、会话 ID 不变、上层业务无感**
- 每次降级必须记录"触发指标快照"（RTT、loss、jitter、候选类型、NAT 类型）

## 3.7 QoS 与拥塞控制

### 拥塞控制算法

- 优先使用 QUIC-BBR（或按场景可切 CUBIC），BBR 在高丢包链路上表现远优于 CUBIC。
- 屏幕流采用自适应码率（ABR）：分辨率/帧率/码率联动。

### 丢包处理策略

| 策略 | 适用场景 | 说明 |
|------|---------|------|
| 关键帧请求 (IDR Request) | 画面严重花屏/卡死 | 请求被控端发送一个完整关键帧 |
| 轻量 FEC（前向纠错） | 弱网（丢包 5%~15%） | 在发送端添加冗余数据，接收端可恢复少量丢包 |
| NACK（选择性重传） | 关键数据丢失 | 仅重传丢失的关键包，不重传所有包 |
| 自适应码率 | 带宽持续不足 | 降低分辨率/帧率/码率 |

### 优先级调度

```
P0: 输入事件（键鼠）  → 绝对优先，永不排队
P1: 屏幕增量帧/音频   → 高优先，允许少量丢帧
P2: 文件块           → 背景填充，限速不抢占
```

## 3.8 加密与密钥协商

| 层次 | 机制 | 说明 |
|------|------|------|
| 传输层 | QUIC (TLS 1.3) | 默认加密，0-RTT/1-RTT 握手 |
| 身份层 | 设备长期密钥 + 会话临时密钥 | PFS（前向保密） |
| 授权层 | 短时会话令牌 | 一次一会话、最小权限、短 TTL |
| 中继模型 | 默认密文转发 | 中继不可解业务内容（E2EE） |
| 密钥轮换 | 长会话定时 rekey | 异常时强制换钥 |
| 审计 | 记录"谁在何时控制谁" | 不记录敏感画面内容 |

## 3.9 断线重连与路径迁移

### 快速重连

- 会话令牌短时续签 + 断线窗口（如 30~120 秒内可恢复）。
- 超出窗口则需全量重建会话。

### Make-Before-Break

- 先建立新路径，确认可用后再切流。
- 减少黑屏/操作中断时间。

### 路径切换触发条件

- RTT 超阈值持续 N 秒
- 丢包率连续超阈
- 带宽跌破编码下限
- 网络接口变化（Wi-Fi → 4G）

### 会话不中断目标

- 控制通道先恢复（键鼠可用）
- 屏幕流随后平滑恢复（可先降分辨率再提升）

## 3.10 TURN / 自建中继职责边界

### TURN 标准职责

- 提供中继候选地址
- 在 P2P 不可达时转发流量（UDP/TCP/TLS）

### 自建中继（建议）

| 职责 | 说明 |
|------|------|
| 多地域接入与就近调度 | 降低跨区 RTT |
| 中继层 QoS | 优先级、限速、反滥用 |
| 可观测性增强 | 会话级链路指标 |
| 安全控制 | DoS 防护、令牌校验、IP 信誉 |
| E2EE 透传 | 中继只见密文，不接触明文 |

### 中继选择策略

- 优先选择 RTT 最低的中继节点（就近调度）。
- 支持故障自动切换到备选区域。
- 中继带宽成本高于 P2P，应作为兜底而非默认。

## 3.11 日志、监控与可观测性

### 控制面日志

- 会话生命周期事件：create / accept / reject / start / end
- 鉴权决策、策略命中、失败原因码

### 数据面指标

| 指标类别 | 具体指标 |
|---------|---------|
| 建链质量 | P2P 成功率、中继率、首帧时延 |
| 交互体验 | 输入到响应时延、平均 RTT、95/99 分位 |
| 传输质量 | 丢包率、抖动、有效带宽、重传率 |
| 可用性 | 黑屏时长、路径切换次数、重连成功率、会话中断率 |

### 追踪与告警

- 全链路 `trace_id` 贯穿控制面与数据面。
- 告警分层：区域故障、运营商故障、版本回归、特定 NAT 类型劣化。
- 隐私合规：日志脱敏、最小留存、按租户隔离。

### 核心统计维度

- 按网络环境维度（家宽/企业网/蜂窝网）
- 按地区、运营商
- 按 NAT 推断类型
- 按客户端版本

## 3.12 各阶段失败码与原因

建议统一编码：`RC-阶段-序号`，便于排障聚合。

| 阶段 | 失败码 | 常见原因 | 建议动作 |
|------|--------|---------|---------|
| 设备上线 | `RC-1001 AUTH_INVALID` | Token 失效/签名错误 | 刷新 token，重登 |
| 设备上线 | `RC-1002 DEVICE_CONFLICT` | 设备 ID 冲突或重复登录 | 按策略踢旧会话 |
| 设备上线 | `RC-1003 HEARTBEAT_TIMEOUT` | 心跳超时、网络抖动 | 快速重注册 |
| 会话创建 | `RC-2001 TARGET_OFFLINE` | 被控端不在线 | 提示稍后重试 |
| 会话创建 | `RC-2002 POLICY_DENY` | 权限不足、风控拒绝 | 返回明确拒绝原因 |
| 会话创建 | `RC-2003 SESSION_QUOTA` | 并发会话超限 | 排队或拒绝 |
| 候选交换 | `RC-3001 CANDIDATE_GATHER_TIMEOUT` | STUN 超时、网卡异常 | 缩短等待并启用 relay 预分配 |
| 候选交换 | `RC-3002 CANDIDATE_EMPTY` | 无可用 UDP/接口受限 | 直接走中继 |
| 候选交换 | `RC-3003 SIGNALING_TIMEOUT` | 信令丢失或延迟高 | 重发 + 幂等去重 |
| 打洞/探测 | `RC-4001 PUNCH_TIMEOUT` | NAT 映射未建立 | 换候选对继续 |
| 打洞/探测 | `RC-4002 CHECK_FAILED` | 连通性检查全失败 | 触发中继兜底 |
| 建链握手 | `RC-4101 HANDSHAKE_TIMEOUT` | 握手往返超预算 | 降低首包负载/改走 relay |
| 建链握手 | `RC-4102 FINGERPRINT_MISMATCH` | 指纹不一致、潜在劫持 | 立即终止并告警 |
| 业务通道 | `RC-5001 CHANNEL_OPEN_TIMEOUT` | 多路通道建立失败 | 单通道降级重试 |
| 业务通道 | `RC-5002 CODEC_NEGOTIATION_FAIL` | 编解码能力不匹配 | 回退到基础编码 |
| 保活 | `RC-5101 KEEPALIVE_LOST` | 连续保活丢失 | 进入重连 |
| 重协商/迁移 | `RC-6001 RENEGOTIATION_CONFLICT` | 双端并发重协商冲突 | 版本号仲裁 |
| 路径迁移 | `RC-6002 MIGRATION_FAIL` | 新路径探测失败 | 保留旧路或转 relay |
| 降级中继 | `RC-6101 RELAY_ALLOC_FAIL` | TURN 配额不足/鉴权失败 | 切备用 relay 区域 |
| 重连 | `RC-7001 RESUME_TOKEN_EXPIRED` | 恢复票据过期 | 全量重建会话 |
| 重连 | `RC-7002 RETRY_BUDGET_EXHAUSTED` | 退避重试耗尽 | 结束会话并提示重发起 |
| 结束 | `RC-8001 REMOTE_HANGUP` | 对端主动结束 | 正常结束流程 |

## 3.13 工程参数设计：超时、重试、降级门限

### 探测超时

采用"静态基线 + 动态修正"：

- 基线值：`T_probe_base = 800ms`（宽带）、`1200ms`（移动网络）
- 动态修正：`T_probe = clamp(600ms, 4 × SRTT + 300ms, 2000ms)`
- 首次建链总预算：`T_connect_budget = 3s ~ 5s`（交互型远控建议 ≤ 4s）
- 候选收集窗口：`1.0s`（trickle 模式可先发首批再补齐）

### 重试策略

| 场景 | 重试次数 | 间隔 | 抖动 |
|------|---------|------|------|
| 打洞重试 | 每候选对 6~8 次 | 80~120ms | ±20% |
| 连通性检查 | 每候选对 3 次 | 失败即切下一对 | — |
| 信令重发 | 2 次（幂等消息 ID） | — | — |
| 重连退避 | 0.5s → 1s → 2s → 4s → 8s | 上限 8s | 总预算 20~30s |

### 降级门限（P2P → Relay）

| 门限类型 | 条件 | 动作 |
|---------|------|------|
| 硬门限 | 连通性检查全失败 | 立即降级 |
| 硬门限 | 连续 3 个保活周期无响应 | 立即降级 |
| 硬门限 | 传输层 close/reset | 立即降级 |
| 软门限 | 丢包率 > 15% 持续 3s | 降级 |
| 软门限 | RTT > 350ms 持续 5s | 降级 |
| 软门限 | 卡顿率 > 20% 持续 3s | 降级 |

### 回切门限（Relay → P2P）

| 条件 | 值 |
|------|-----|
| 最短驻留时间 | 30s（防抖） |
| 后台探测成功次数 | 连续 3 次 |
| 质量提升阈值 | 时延下降 > 20% |

### 参数落地建议

- 参数按网络类型分层：`LAN`、`Wi-Fi`、`4G/5G` 各自配置。
- 关键阈值支持服务端热更新（无需发版）。
- 核心 SLO 指标：`P95 建链时延`、`P2P 成功率`、`中继占比`、`重连成功率`。

## 3.14 实施路线图

### Phase 1：基础可用

- P2P 直连（STUN + UDP 打洞）
- TURN UDP 中继兜底
- 基础 QoS（自适应码率、保活）
- 控制面核心服务（鉴权、信令、会话）

### Phase 2：体验优化

- QUIC 传输层替换裸 UDP
- 路径迁移 + 无感切换
- TCP/TLS/WebSocket 兜底完善
- 多地域中继部署 + 就近调度
- 完善状态机与错误码体系

### Phase 3：智能运营

- 全量可观测性（链路指标、用户维度聚合）
- 智能路由优化（基于历史数据的路径预测）
- 自动故障演练与回放
- 端到端加密审计
- 成本优化（P2P 成功率提升 → 中继带宽成本下降）

---

> **文档版本**：v1.0  
> **最后更新**：2026-04  
> **适用范围**：远程控制软件底层网络链路设计与内网穿透技术参考
