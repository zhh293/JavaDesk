# 分布式化改造方案

## 一、背景与目标

当前 `server-signaling` / `server-relay` 的若干状态强依赖单机内存(`ConnectionRegistry` 的 `Channel` 表、`SessionManager` 的会话记录、`RelayNodeRegistry` 的在线表与评分、`SessionService` 的会话级 Map、relay 侧会话席位表),多节点部署下会出现邀请路由断链、两端 JOIN 到不同 relay、质量评分割裂等问题。

目标:在**保持 dev 单机行为不变**的前提下,为 prod 提供一条可横向扩展的分布式路径,分三块改造——

1. **信令侧**:中继节点注册表改用 Nacos AP + Distro 协议在信令集群内同步(完全 Redis-free);跨节点信令路由 / 会话元数据 / device 在线仍走 Redis(CP 倾向,保证路由不丢)。
2. **客户端**:relay 节点拉取 + 本地缓存 + Dubbo Cluster 风格分层调用链(Registry → Directory → Cluster → Invoker → Exchanger → Transport),内置熔断状态机与 Failover 策略。
3. **LB / 网关接入**:**本期暂不做,留待 TODO**(见第七节)。客户端仍直连单台信令地址,分布式部署时由运维在客户端配置切到具体节点;真正上多节点生产前再补 LB 接入方案。

非目标:不改 server-relay 进程内的 `RelaySessionRegistry` / `StreamRelaySessionRegistry`(转发用的是本进程 socket,天然每节点独立);不引入 Spring Cloud Gateway(信令已有 Spring Security + JWT,无需叠加)。

## 二、整体架构

```
            客户端(直连某台信令节点)
                │
                │ REST 拉取 relay 列表 + 信令长连接 + Auth REST
                ▼
   ┌──────────────────┐                  ┌──────────────────┐
   │   Signaling A    │◄── Distro 同步 ──►│   Signaling B    │
   │  localShard      │                   │  localShard      │
   │  + mirror        │                   │  + mirror        │
   └────────┬─────────┘                  └────────┬─────────┘
            │                                       │
            │   共享 Redis(device 在线 / session / 路由 / message bus)
            └─────────────────────┬──────────────────┘
                                  │
                                  ▼
                       ┌──────────────────┐
                       │ Relay X / Y / Z  │ ...
                       └──────────────────┘
                                  ▲
                                  │ JOIN(凭信令签发的 token)
                                  │
                              ┌───────┐
                              │Client │ 本地 Directory + CircuitBreaker + Failover
                              └───────┘
```

> **注**:本期不引入 LB,客户端直连某台信令节点。多信令节点部署下,客户端配置里指定一个目标节点;跨节点路由由 `RoutingTable + MessageBus` 内部完成,对客户端透明。LB / 网关接入见第七节 TODO。

## 三、设计模式

| 模式 | 落点 |
|------|------|
| Strategy | 每个状态存储抽接口,配 InMemory* / Distro* 或 InMemory* / Redis* 双实现,`@Profile` 切换 |
| Abstract Factory | `ClusterProfile` 统一注入一族实现,防止混搭崩盘 |
| Facade | `SessionService` 退化为薄编排,不持有 Map |
| Observer / Pub-Sub | `MessageBus` 做跨节点信令投递(dev 直通,prod Redis Pub/Sub) |
| Service Registry + Pull | `RelayRegistryClient` 客户端拉取 + 缓存 + 定时轮询(Nacos SDK 风格) |
| Cluster Invoker | `RelayClusterInvoker` 层层封装:Registry → Directory → Cluster → Invoker → Exchanger → Transport(Dubbo RPC 风格) |
| Circuit Breaker | 每 relay 一个状态机(CLOSED / OPEN / HALF_OPEN),Directory 过滤 OPEN |

## 四、信令侧 Distro(AP 注册表)

### 4.1 数据模型

`RelayNode` 现有字段不变。新增**评分同步字段**作为 Distro payload 的一部分:

```
DistroPayload {
    nodeId: 信令节点 ID(payload 来源)
    version: 单调递增(用于对账去重)
    relayNodes: List<RelayNode>     // owner 负责的 relay 全量快照
    qualities: Map<nodeId, {loadEwma, successEwma, lastUpdate}>
}
```

### 4.2 负责分片

- 每个 relay 心跳报给某台信令(经 LB 随机命中),那台信令是该 relay 的 **owner**。
- owner 把 relay 写入本地 `localShard`(Redis 不再参与)+ 落库 `relay_node` 表(已有,幂等 upsert)。
- 非 owner 节点收到 relay 心跳 → 转发给当前 owner(若已知)或拒绝(让 relay 重试,经 LB 可能命中 owner)。**简化方案**:任意节点都能接收心跳并写入 localShard + 镜像,owner 概念仅用于"谁负责对外同步这条数据"。具体见 4.5。

### 4.3 Distro 同步协议

**全量快照同步**(relay 量级小,几十~几百,不做 delta):

- **on-change push**:relay 上下线 / 评分变化超过阈值 → owner `POST /internal/distro/relay` 推给所有 peer。
- **periodic reconcile**:每 5s,每节点 `GET /internal/distro/relay?from=nodeId` 拉取每个 peer 的本地分片,与镜像做差分,补齐遗漏。
- **去重**:`version` 单调递增,旧版本 payload 丢弃。

### 4.4 质量评分同步

**结论:同步**。原因:

- 不同步 → 不同信令节点评分不一致 → `selectBest` 跨节点结果不同 → 同一会话双方可能被分到不同 relay → 中继不通(用户此前指出的关键洞)。
- 同步成本可忽略:每次 Distro payload 多带几 KB 评分数据,relay 数量级小。

每节点维护的 `successEwma` 仍由本节点观测填充,但通过 Distro 同步给 peer。peer 接收时取**所有节点观测的加权平均**(按各自样本数),得到全局一致视图。

### 4.5 心跳落地策略(简化)

为避免 "relay 心跳经 LB 命中非 owner 节点" 的复杂性,采用**任意节点接受 + 全网同步**模型:

- 任意信令节点收到 relay 心跳 → 写 `localShard` + 落库。
- Distro 把 `localShard` 同步到所有 peer。
- peer 收到 → 合并到 `mirror`(同 relay 取 `lastHeartbeatAt` 最新者)。
- 评分 `recordAllocResult` 也在本地 `localShard` 更新 + Distro 同步。

效果:任意节点都能服务"分配 relay"请求,视图最终一致(AP)。

### 4.6 文件改动(server-signaling)

新增:

```
session/
├── RelayNodeRegistry.java                # 改成接口
├── InMemoryRelayNodeRegistry.java        # @Profile("!prod"),原类内容平迁
├── DistroRelayNodeRegistry.java           # @Profile("prod"),localShard + mirror

distro/
├── DistroPayload.java                     # 快照 record
├── DistroPeerRegistry.java                # peer 列表(配置注入)
├── DistroSyncClient.java                  # 拉取 peer 分片(JDK HttpClient)
├── DistroSyncServer.java                  # 暴露 /internal/distro/relay
├── DistroSynchronizer.java                # onChange push + 周期 reconcile
└── DistroMetrics.java                     # sync 次数 / 延迟 / 冲突计数
```

修改:

- `RelayController`:新增 `GET /internal/distro/relay`、`POST /internal/distro/relay`(内网 ACL + mTLS,不对外)
- `RelayManager`:依赖从具体类改依赖 `RelayNodeRegistry` 接口
- `SignalingProperties`:新增 `distro.peers`(逗号分隔)、`distro.sync-interval-ms`(默认 5000)、`distro.push-on-change`(默认 true)
- `SignalingApplication`:启动时初始化 `DistroSynchronizer`

### 4.7 限制与回退

- Distro 同步延迟典型 < 5s,极端(网络分区)下可能更长。期间客户端拉到旧视图 → 仍能选到已下线 relay → 熔断器快速打开切走,可接受。
- peer 列表是启动配置注入,**不支持运行时增减信令节点**(简化);若要动态扩容,后续做服务发现(信令节点之间互相发现)。

## 五、客户端 Dubbo Cluster 分层

### 5.1 分层映射

| Dubbo 层 | 本项目类 | 职责 |
|---------|---------|------|
| Registry | `RelayRegistryClient` | 定时拉 `/api/relay-nodes`(JWT 鉴权)+ 本地缓存 |
| Directory | `RelayDirectory` | 持有 `List<RelayInvoker>`,Registry 推送时刷新 |
| Cluster | `RelayClusterInvoker` | Failover 策略:挑 invoker → 失败换下一个 |
| Invoker | `RelayInvoker` | 包装一个 `RelayEndpoint` + `CircuitBreaker` + 评分 |
| Filter | (内置 Invoker) | 熔断判定、负载统计 |
| Exchanger | `RelayExchanger` | 连接池 / 复用 / 空闲回收(包装现有 `Relay*TransportChannel`) |
| Transport | `RelayTransportChannel` / `RelayTcpTransportChannel` / `RelayWsTransportChannel` | 不动 |
| Client | 现有 Netty 底层 | 不动 |

### 5.2 熔断状态机

```
                       连续 K 次失败
       ┌──────────┐ ───────────────► ┌──────────┐
       │  CLOSED  │                    │   OPEN   │
       └──────────┘ ◄─────探测成功──── └──────────┘
            ▲                              │
            │                              │ 冷却 T 秒
            │                              ▼
            │                          ┌────────────┐
            └──────── 探测成功 ──────────│ HALF_OPEN  │
                                       └────────────┘
                                          │ │
                                  探测失败─┘ └─探测成功→CLOSED
                                              探测失败→OPEN(重置 openedAt)
```

- **CLOSED**:正常调用。失败计数滑动窗口(1 分钟),连续 K 次 → OPEN。
- **OPEN**:Directory 过滤,不调用。冷却 T 秒 → HALF_OPEN。
- **HALF_OPEN**:放 1 个探测请求过去。成功 → CLOSED;失败 → OPEN(重置 `openedAt`)。

阈值(默认值,可配):连续失败 K=5,冷却 T=10s,滑动窗口 60s。

### 5.3 Failover 策略

`RelayClusterInvoker.invoke(sessionId, pathType)`:

```
1. directory.list(pathType) 过滤 OPEN
2. 若 EMPTY → 返回失败(上层降级到下一协议)
3. 按 load + success 评分排序,取首
4. invoker.invoke() → exchanger.acquire() → RelayTransportChannel
5. JOIN 失败/超时 → breaker.recordFailure()
6. 失败 → 取下一 invoker 重试(最多 N=3 次)
7. 全失败 → 返回失败
```

### 5.4 Registry 拉取

- 客户端启动拉一次,后续每 10s 全量刷新(增量优化延后)。
- 入口:复用 `SignalingClientConfig.host/port` 拼成 `http://host:port/api/relay-nodes`,`RelayRegistryClient` 短超时(2s)+ 失败重试。
- 缓存:`Caffeine` TTL 30s,断网期间用缓存兜底(标记 stale 但可用)。
- 鉴权:复用 `accessToken`(JWT)。多信令节点部署下,客户端配置指向具体节点(LB 接入见第七节 TODO)。

### 5.5 Exchanger 连接复用

- 每 `RelayEndpoint` + pathType 一个连接(`RelayTransportChannel` 等)。
- 同会话不跨 pathType 复用(降级时新建)。
- 空闲 > 60s 主动关闭,下次重连。
- 失败连接不回池,直接释放。

### 5.6 文件改动(client)

新增:

```
client/relay/
├── RelayEndpoint.java                    # record:host/region/udp+tcp+ws/tls/load
├── RelayRegistryClient.java             # 定时拉取 + 缓存(Caffeine)
├── RelayDirectory.java                   # 维护 List<RelayInvoker>,刷新
├── CircuitBreaker.java                   # 状态机 + 滑动窗口
├── RelayInvoker.java                     # 包装 endpoint + breaker + invoke()
├── RelayExchanger.java                   # 连接池 / 复用 / 空闲回收
├── RelayClusterInvoker.java              # Failover 策略
└── RelayClusterConfig.java               # 拉取周期 / 熔断阈值 / 超时

client/signaling/
└── SignalingClientConfig.java            # 不新增字段(本期);LB 接入时再扩 lbUrl/lbEndpoints
```

修改:

- `ClientConnectionManager`:降级阶梯 P2P→Relay-UDP→Relay-TCP→Relay-WS 里,**所有 Relay-* 阶段统一走 `RelayClusterInvoker.invoke()`**,不再直接 `new RelayTransportChannel(host, port, ...)`。中继令牌 (`RelayAllocResp.token`) 由信令签发,Cluster 选定 relay 后把令牌塞给 TransportChannel。
- `RemoteControlClient.start()`:启动 `RelayRegistryClient` 定时器。

### 5.7 协议变化:relay 列表接口对客户端可见

现有 `/internal/relay-nodes` 是内网接口(ACL 防护)。客户端拉取需新增:

- `GET /api/relay-nodes`(JWT 鉴权,返回在线 relay 列表的**精简视图**:host、region、端口、tls、loadRatio。**不返回** secret 等内部字段)。

`RelayController` 拆分:

- `/internal/relay-nodes/heartbeat`(relay → 信令,内网 ACL)
- `/internal/relay-nodes`(运维,内网 ACL)
- `/api/relay-nodes`(客户端,JWT)
- `/internal/distro/relay`(信令 peer 之间,内网 ACL + mTLS)

## 六、跨节点信令路由(CP,Redis)

RelayNodeRegistry 走 Distro。其余跨节点状态仍走 Redis(CP 倾向):

### 6.1 抽象接口

```
RoutingTable           # deviceId → nodeId
  ├── InMemoryRoutingTable     # dev,恒返回 self
  └── RedisRoutingTable         # prod,读 device:online:{id}(已有 key)

SessionStore           # sessionId → SessionMetadata
  ├── InMemorySessionStore     # dev
  └── RedisSessionStore         # prod,TTL 30 分钟

MessageBus             # 跨节点信令广播
  ├── InMemoryMessageBus       # dev,直接走本机 ConnectionRegistry(短路)
  └── RedisMessageBus          # prod,Pub/Sub,节点 ID 订阅自己的 channel
```

### 6.2 SessionMetadata 合并

`SessionService` 现有的 `relayTokens` / `sessionRelayNode` 两个 Map 合并进 `SessionMetadata`:

```
SessionMetadata {
    sessionId, controllerDeviceId, agentDeviceId,
    controllerNodeId, agentNodeId,
    relayToken, relayNodeId,
    createdAt, updatedAt
}
```

Redis 端用一个 hash key `session:{sessionId}`,TTL 跟随会话生命周期。Candidate 频率高,`SessionStore.get` 加 Caffeine 本地缓存(TTL 1~2s),Pub/Sub 失效通知。

### 6.3 投递逻辑

`SessionService.handleCandidate / handleInviteResp / handlePathSwitch` 改写:

```
meta = sessionStore.get(sessionId)
peerNodeId = meta.peerNodeIdOf(fromDeviceId)
if peerNodeId == selfNodeId:
    ch = connectionRegistry.channelOf(peerDeviceId)
    if ch != null: ch.writeAndFlush(signal)
else:
    messageBus.publish(peerNodeId, signal)
```

dev 下 `RoutingTable` 恒返回 self,`MessageBus` 短路直接走本机 `ConnectionRegistry`,行为完全不变。

### 6.4 文件改动(server-signaling)

新增:

```
session/
├── RoutingTable.java                     # 接口
├── InMemoryRoutingTable.java             # @Profile("!prod")
├── RedisRoutingTable.java                # @Profile("prod")
├── SessionStore.java                     # 接口 + SessionMetadata record
├── InMemorySessionStore.java            # @Profile("!prod")
├── RedisSessionStore.java               # @Profile("prod")
├── MessageBus.java                       # 接口
├── InMemoryMessageBus.java               # @Profile("!prod")
├── RedisMessageBus.java                  # @Profile("prod")
├── NodeIdProvider.java                   # dev="dev",prod=hostname/UUID
└── ClusterProfile.java                   # Abstract Factory,统一注入
```

修改:

- `SessionManager`:职责被 `SessionStore + ConnectionRegistry` 取代,删除或保留薄壳。
- `SessionService`:删本地 Map,改读 `SessionStore`;peer 投递走 `MessageBus.publishOrLocal`。
- `SignalServerHandler`:连接建立时 `routingTable.register(deviceId, selfNodeId)`(prod 才有实际效果)。
- `SignalingApplication`:启动注入 `NodeIdProvider`。

## 七、LB / 网关接入(TODO,本期不做)

本期客户端直连信令节点,不引入 LB / 网关。等真正上多节点生产前再补,届时需要:

- **流量分流**:REST 拉取 + Auth 走 L7(HTTP LB),信令长连接走 L4(TCP LB)或 L7 + TLS passthrough;Distro peer 同步走内网直连 + mTLS。
- **客户端配置扩展**:`SignalingClientConfig` 增加 `lbUrl / lbHost / lbPort / lbEndpoints`(无 LB 时直接配后端列表,客户端轮询)。
- **关键属性**:Distro(relay 全量镜像)+ Redis(device / session / message bus 共享)共同保证任意信令节点都能服务任意客户端请求,LB round-robin 即可,无需会话粘性。
- **LB 自身可用性**:自建 keepalived + 两台 nginx/LVS,或云托管 NLB/ALB(多 AZ),或 K8s Ingress Controller 多副本。
- **健康检查**:LB 探 `GET /actuator/health`(已有 actuator 端点),摘掉挂掉的信令节点。

这些字段和接口在 M1~M3 实现里都留出 hook 点(`SignalingClientConfig.host/port` 不硬编码、`RelayRegistryClient` 不直接拼死地址),M4 接入时只改配置 + 加 `lbUrl` 解析,代码改动最小。

## 八、完整时序

### 8.1 客户端拉取 relay 列表

```
客户端启动:
  AuthApiClient.login()  → accessToken
  RelayRegistryClient.pull():
    GET http://{signalingHost}:{signalingPort}/api/relay-nodes (Authorization: Bearer <token>)
    → 200 OK, List<RelayEndpoint>
    → 写入 Caffeine 缓存 + 推送 RelayDirectory
  scheduler 每 10s 重复 pull
  断网:用 Caffeine 缓存(stale 标记)兜底
```

> `signalingHost/Port` 取自 `SignalingClientConfig`,本期与信令长连接同一地址。多信令节点部署下,客户端配置指向要连的那台节点;LB 接入见第七节 TODO。

### 8.2 控制端发起连接(分布式)

```
1. AuthApiClient.login()              → accessToken
2. SignalingClient.connect(host:port)  → 直连某台信令节点 A
3. onConnected → sendRegister()
4. RegisterResp → deviceId
5. DeviceInfoClient.fetch(targetCode)  → 走同一信令节点(或另一台,视部署)
   → 目标在线 + 公钥
6. CryptoService.sealConnectionPassword(...)  → 密文
7. send(InviteReq)  → 节点 A 处理
   节点 A 查 RoutingTable(targetDeviceId):
     if targetNodeId == A: 本机 ConnectionRegistry.channelOf → 直接发
     else: MessageBus.publish(targetNodeId, signal)
8. 节点 B(被控端所在)收到 MessageBus 推送 → 本机 ConnectionRegistry.channelOf → 转给被控端
9. 被控端弹 UI 确认 → acceptInvite → 沿原路回 InviteResp
10. ICE / 打洞(略)
11. 打洞失败 → 信令侧 handleRelayAlloc:
      DistroRelayNodeRegistry.bestFor(region) → 全局一致的 relay 节点
      签发 token,RelayAllocResp 给双方
12. 客户端收到 RelayAllocResp:
      RelayClusterInvoker.invoke(sessionId, RELAY_UDP)
      → Directory 过滤 OPEN + 按评分挑 1 个 invoker
      → Exchanger 获取 / 创建 RelayTransportChannel
      → JOIN 失败 → 熔断记录 → Failover 换下个 invoker
      → 成功 → 数据面建立
13. 全程信令心跳定时器 send(Heartbeat)
```

## 九、配置变化汇总

### server-signaling(`application.yml`)

```yaml
rc:
  signaling:
    node-id: ${HOSTNAME:node-1}      # prod 注入 hostname
    device-ttl-seconds: 60
  distro:
    enabled: ${rc.cluster:false}       # prod=true
    peers: ${RC_DISTRO_PEERS:}        # 逗号分隔,内网直连
    sync-interval-ms: 5000
    push-on-change: true
  cluster:                            # 集群模式开关
    enabled: ${rc.cluster:false}
spring:
  profiles:
    active: ${RC_PROFILE:dev}        # dev=单机 in-mem,prod=Distro+Redis
  redis:
    host: ${RC_REDIS_HOST:}
```

### client(`client.conf`)

```
signaling.host=127.0.0.1              # dev;prod 指向具体信令节点
signaling.port=8443
relay.registry.pull-interval-ms=10000
relay.cluster.failover-max-retry=3
relay.breaker.failure-threshold=5
relay.breaker.cooldown-ms=10000
relay.breaker.window-seconds=60
```

> `signaling.lb.*` 字段本期不加,留到第七节 TODO 落地时再扩。

### server-relay(`env`)

不变。relay 仍按现有 `RelayConfig.fromEnv()` 配置。`rc.relay.signaling-url` 指向某台具体信令节点的 `/internal/relay-nodes/heartbeat`;多信令节点部署时,relay 报给任一台都能经 Distro 同步到全网(见 4.5)。

## 十、落地顺序与里程碑

按"风险递增、依赖递增"排序,每步可独立验证:

### M1:客户端熔断 + Cluster 调用链(纯客户端,零外部依赖)

**目标**:把现有"挑一个 relay 节点"的硬编码改为 Dubbo Cluster 风格分层调用 + 熔断。

- 新增 `client/relay/` 全部文件
- `ClientConnectionManager` 降级链里 Relay-* 阶段改走 `RelayClusterInvoker`
- dev 下信令仍是单点,`/internal/relay-nodes` 暴露给客户端(临时 JWT 鉴权)
- 单机功能验证:手动停一个 relay,看 Failover 是否切走

### M2:信令侧 Distro(集群内 relay 注册表同步)

**目标**:多信令节点部署,relay 列表全局一致。

- 新增 `server-signaling/distro/` 全部文件
- `RelayNodeRegistry` 改接口 + `InMemory` / `Distro` 双实现
- `RelayController` 新增 `/internal/distro/relay` + `/api/relay-nodes`
- 评分同步:relay 自报 load + 各节点观测的 success 都走 Distro
- 验证:双信令节点部署,relay 心跳报给 A,B 的 `selectBest` 视图一致

### M3:跨节点信令路由(Redis)

**目标**:多信令节点下邀请/候选/中继分配全链路打通。

- 新增 `RoutingTable` / `SessionStore` / `MessageBus` 抽象 + 双实现
- `SessionService` 改写 peer 投递逻辑
- `SignalServerHandler` 注册路由表
- 验证:控制端连 A,被控端连 B,邀请能送到,会话能建立,中继能 JOIN

每步完成后**不破坏 dev 单机行为**(dev 下 `@Profile("!prod")` 实现短路所有分布式路径)。

> **M4(LB 接入)留待 TODO**,见第七节。M1~M3 完成后,真正上多节点生产前再补。

## 十一、风险与缓解

| 风险 | 缓解 |
|------|------|
| `@Profile` 混搭崩盘(in-mem SessionStore + Redis RoutingTable) | `ClusterProfile` Abstract Factory 统一注入,启动校验一致性 |
| Distro 同步延迟期间客户端拉到旧 relay 视图 | 客户端熔断器快速打开切走,5s 内自愈 |
| Redis Pub/Sub at-most-once,节点重启丢消息 | SessionEnd 走 Redis 主动 DEL + 双方连接断开自然清,不依赖消息必达;Candidate / InviteResp 失败由超时重试兜底 |
| `SessionStore` 读放大(Candidate 频率高) | Caffeine 本地缓存 + Pub/Sub 失效通知 |
| relay 心跳经 LB 命中非 owner 节点 | 简化方案:任意节点接受心跳 + 全网 Distro 同步,无 owner 概念 |
| peer 列表静态配置,不支持运行时扩信令节点 | v1 限制,后续做服务发现(信令节点互发现) |
| Distro 同步自身挂掉(网络分区) | mirror 用本地最后已知数据继续服务,触发告警,人工介入 |

## 十二、回滚

每个里程碑独立可回退:

- M1 回退:`ClientConnectionManager` 改回直接 `new RelayTransportChannel`,删除 `client/relay/`。
- M2 回退:`@Profile("prod")` 切回 `RedisRelayNodeRegistry`(保留 v1 实现)或回 `InMemoryRelayNodeRegistry` 单点部署。
- M3 回退:`@Profile` 切回 in-mem,SessionStore / RoutingTable / MessageBus 走 dev 实现(单机行为)。

## 十三、不在本次范围

- server-relay 进程内会话表改造(无需,转发本就是本进程 socket)
- Spring Cloud Gateway 接入(信令已有 Spring Security,无价值)
- 信令节点服务发现(peer 列表静态配置足够 v1)
- relay 节点服务发现(走 Distro 注册表已经覆盖)
- session 元数据走 Raft / CP 强一致(本期保持 Redis AP,够用)
- 客户端 relay 列表增量同步(全量足够,relay 数量级小)
