# JavaDesk 项目架构、流程与底层原理

> 文档基线：V2 分布式化重构后的当前代码
> 读者：开发、测试、SRE、安全与架构评审人员
> 目标：解释代码“为什么这样分层、状态如何流转、故障时凭什么不脑裂”，而不只是罗列类名。

## 1. 系统解决什么问题

JavaDesk 是自托管远程控制系统。它需要同时满足两类看似冲突的目标：

- 尽量走 P2P，减少中心带宽和交互延迟；
- 在 NAT、运营商防火墙、UDP 不可达、节点宕机和客户端重连时仍可达；
- 信令和 relay 可以横向扩展，但一个会话不能因节点视图不同选出两条“当前路径”；
- relay 必须能转发数据，却不应知道屏幕、键鼠、文件等业务明文；
- 路径切换不能要求五类业务服务各自重新绑定底层 socket。

因此系统不是“一个 Netty 服务 + 两个客户端”，而是三个互相独立的平面：

```text
控制面：认证、设备租约、邀请、会话成员、路由仲裁、恢复快照
会话面：每个 session 的状态机、routeEpoch、PREPARE/READY/COMMIT
数据面：P2P 或指定 relay 上的加密业务帧
```

## 2. 总体架构

```text
 Controller Client                                  Agent Client
 ┌──────────────────┐                          ┌──────────────────┐
 │ ConnectionMgr    │                          │ ConnectionMgr    │
 │ SwitchableCh     │                          │ SwitchableCh     │
 │   └─ SecureCh    │                          │   └─ SecureCh    │
 │      └─ P2P/Relay├──────── data plane ──────┤      P2P/Relay   │
 └────────┬─────────┘                          └────────┬─────────┘
          │ TLS signaling                              │
          └──────────────────┬─────────────────────────┘
                             ▼
              ┌──────────────────────────────┐
              │ Signaling A / B / C          │
              │ LocalConnectionRegistry      │  只保存本机 Channel
              │ ConnectionLeaseStore         │  device → node/epoch
              │ SessionStore                 │  成员/状态/version
              │ SessionRouteCoordinator      │  唯一路由仲裁者
              │ SignalRouter                 │  本地短路/跨节点投递
              └───────┬────────┬────────┬─────┘
                      │        │        │
                      ▼        ▼        ▼
                   Redis     MySQL       Nacos
              lease/CAS/质量  audit   Relay ephemeral 实例
                 streams
                      │
          ┌───────────┴────────────────────┐
          ▼                                ▼
 ┌──────────────────┐            ┌──────────────────┐
 │ Relay A           │            │ Relay B           │
 │ ticket verify     │            │ ticket verify     │
 │ epoch/role seats  │            │ epoch/role seats  │
 │ ciphertext relay  │            │ ciphertext relay  │
 └──────────────────┘            └──────────────────┘
```

核心边界是：发现系统只提供候选，`SessionRouteCoordinator` 才能决定某个 session 使用哪条路径。客户端的 circuit breaker 只描述“我到指定节点可能不可达”，不能把本地选择升级成双方共同事实。

## 3. 模块职责

### 3.1 `common`

`common` 是协议与密码学边界：

- `signal.proto`：当前 Netty 长连接 wire，包含注册 fencing、会话快照、relay assignment 和路由提交；
- `signal_v2.proto`：分布式信封、恢复与状态模型定义，供后续网关/内部总线演进；
- `RelayPacketCodecV2`：relay 可见的 session/epoch/path/role/seq 头；
- `OuterTransportFrameCodec`：数据面 outer/inner 两层帧；
- `RelayTicketSigner/Verifier`：Ed25519 relay 票据；
- `SessionKeySchedule/DataNonceFactory/ReplayWindow`：密钥隔离、nonce 唯一性和重放防护。

此模块不依赖 Spring，客户端、信令和 relay 使用同一份协议解释，避免三端复制常量后漂移。

### 3.2 `server-signaling`

信令服务是控制面和会话面的权威入口：

- `ConnectionRegistry`：仅保存当前节点的 Netty `Channel`；
- `ConnectionLeaseStore`：保存可序列化的设备路由和 fencing epoch；
- `SessionStore`：保存不可变成员关系、状态、version 和当前 routeEpoch；
- `SessionAuthorizationService`：统一执行成员、角色和 connectionEpoch 校验；
- `SessionRouteStore`：保存 committed route 与 preparing route；
- `SessionRouteCoordinator`：执行 PREPARE、双 READY、COMMIT；
- `SignalRouter`：本节点直接写 Channel，跨节点写 Redis Stream inbox；
- `SignalInboxConsumer`：deadline、目标 connectionEpoch、dedupe、DLQ 后再投递；
- `AuthorizationHandoffService`：OIDC 回调只下发 60 秒一次性交接码，生产由 Redis 原子 get-and-delete；
- `ProductionSafetyValidator`：生产环境存在 dev secret、默认 nodeId 或无 TLS 时拒绝启动。

开发 profile 使用内存 store，便于确定性测试；生产 profile 使用 Redis Lua CAS，不允许 Redis 故障时回退内存形成双真相源。

SSO 登录成功后，JWT 先短暂保存在服务端，浏览器重定向 URL 只有随机 `code`。前端以 `POST /api/auth/sso/handoff` 兑换一次；第一次读取会原子删除，重放、过期或格式错误均返回未授权。这样 access/refresh token 不会进入浏览器历史、代理 access log 或 Referer。

### 3.3 `server-relay`

relay 是无业务语义的数据面转发器：

- UDP/TCP/WS 运行时均解析 `RelayPacketCodecV2`，以 `(sessionId, routeEpoch, pathType)` 为会话键；
- controller 和 agent 是明确角色席位，不再按“先来的两个地址”猜角色；
- ticket 绑定 relay node、epoch、path、role、device 和 connectionEpoch；
- `TicketReplayGuard` 允许完全相同的 JOIN 重发，拒绝换 nonce 抢占同角色；
- 每个角色只有在 Ed25519 票据验证、node/path/epoch/role 匹配并占座成功后才收到 JOIN_ACCEPTED；信令收到双端 READY 才提交；
- 数据包序号必须递增，旧包和未知端点不转发。

relay 通过受保护的 `/internal/relay-ticket-keys` 拉取并周期刷新 Ed25519 验签公钥，不持有私钥。多个信令节点使用同一生产密钥集，支持按 keyId 滚动轮换；即使单个 relay 被攻陷，也不能签发全网合法票据。

Relay 自身以 ephemeral instance 注册到 Nacos。Nacos metadata 只放 nodeId、region、UDP/TCP/WS 端口、TLS 和容量等低频能力；activeSessions、CPU、带宽、direct-memory 以及 `region:provider:path` 网络成功率/RTT/丢包 EWMA 进入 Redis。这样不会把高频指标写成注册中心元数据风暴，同时所有信令节点看到同一评分输入。

### 3.4 `client`

客户端分为四层：

```text
业务层：screen / input / audio / file / clipboard
稳定层：SwitchableTransportChannel
安全层：SecureTransportChannel(epoch, direction keys, replay window)
路径层：P2P QUIC/UDP 或 Relay UDP/TCP/WS
```

`SwitchableTransportChannel` 是业务唯一绑定点。新 route COMMIT 时，它先把 listener 接到新 delegate，再原子替换 active delegate，最后关闭旧路径。小于等于当前 epoch 的错误切换不能覆盖新路径。

当前运行主链由 `ClientConnectionManager` 和每会话 `SessionContext` 直接编排。`SessionActor/SessionReducer` 已实现并通过旧 epoch 事件测试，但尚未接入运行主链，不能把它描述成当前生产路径的串行化保证。

客户端运行主链只执行 assignment 指定的 relay。连接失败时对同一 assignment、同一 nonce 最多尝试三次，等待时间使用指数上限内的 full jitter；仍失败才发送 `RelayFailureReport`。`RelayClusterInvoker` 是已测试的辅助模型，当前主链没有调用它；实际重试和 READY/COMMIT 处理位于 `ClientConnectionManager`。

## 4. 设备上线与连接 fencing

每次信令 TCP 连接都有唯一 `connectionId`，每个设备还有单调递增 `connectionEpoch`。

```text
Register(device=42)
  → Redis INCR rc:v2:device:{42}:connection-epoch = 8
  → 写 lease(connectionId=B, epoch=8, node=signaling-B, TTL=75s)
  → 本节点 ConnectionRegistry 登记真实 Channel
```

心跳和断开不是简单的 `SET TTL` / `DEL key`：

```text
renew 仅当 connectionId=B && epoch=8
delete 仅当 connectionId=B && epoch=8
```

若旧连接 A 的 `channelInactive` 晚到，它携带 epoch=7，Lua 条件删除失败，因此不会把新连接 B 标记离线。这就是 fencing token：不要求网络事件按时间顺序抵达，而是让旧事件失去修改新状态的权限。

本地 `Channel` 不能写 Redis。序列化 Channel 没有意义，跨节点只保存 `device → signaling node + epoch` 指针，最终由目标节点的本地 registry 找真实 Channel。

## 5. 会话创建、邀请与成员鉴权

V2 会话成员由服务端确定，创建后不可变化：

```text
SessionMetadata = {
  sessionId, version, state,
  controllerDeviceId, agentDeviceId,
  controllerConnectionEpoch, agentConnectionEpoch,
  coordinatorNodeId, routeEpoch, TTL
}
```

客户端上报的 role、controller code 和 current path 都不是权威事实。所有会话命令先从已认证 `ConnectionContext` 取得 actor，再执行：

1. session 是否存在且未终止；
2. actor 是否是 controller 或 agent；
3. actor 的 connectionEpoch 是否仍是会话记录的有效代次；
4. 命令角色是否正确，例如邀请决定只能由 agent 执行；
5. expectedVersion / expectedState 是否匹配。

会话结束是幂等终态。先鉴权，再 CAS 进入 ENDED；不能先 `remove(sessionId)` 后验证请求方，否则非成员可以结束别人的会话。当前 `SessionService` 按这一顺序执行。

## 6. P2P 建链原理

### 6.1 NAT 与同一 UDP socket

NAT 通常用五元组建立映射。若候选探测使用 socket A，而真正数据面另开 socket B，公网端口可能变化，之前交换的 candidate 随即失效。

JavaDesk 的 `UdpSocket` 在 STUN、打洞和数据面之间复用同一 `DatagramChannel`。切换到 kwik 时取消 selector key、切换阻塞模式，并把同一个底层 `DatagramSocket` 交给 QUIC 实现。

### 6.2 ICE 的本质

ICE 不是一种传输协议，而是“收集候选 + 组合候选对 + 连通性检查 + 选择路径”的过程：

- host candidate：本机网卡地址；
- server-reflexive candidate：STUN 观察到的公网映射；
- relay candidate：无法直连时的兜底地址。

P2P 成功后，可靠业务使用 QUIC stream，视频和音频使用 QUIC datagram。UDP 整体被阻断时，继续从 QUIC 降级到“自定义 UDP”没有意义，因为二者依赖同一 UDP 可达性；因此下一层是 relay。

## 7. 路由迁移状态机

### 7.1 为什么需要服务端仲裁

两端到同一 relay 的网络条件可能完全不同，候选快照时间也不同。同步节点列表只能改善选择质量，不能证明双方选中同一节点。

V2 使用一个服务端状态机：

```text
COMMITTED(epoch=N, old path)
        │ RelayAllocReq（首次 P2P 失败）
        │ 或 RelayFailureReport（运行期失败/劣化）
        ▼
PREPARING(epoch=N+1, assignment=A, deadline=T)
        ├─ controller ticket → Controller
        └─ agent ticket      → Agent
                 │
             本端 JOIN_ACCEPTED
                 │
       READY(controller) / READY(agent)
                 ▼
COMMITTED(epoch=N+1) → RouteCommit → stable delegate swap
                 ▼
             retire epoch=N
```

### 7.2 CAS 规则

`SessionRouteStore.prepare` 同时检查：

- requestId 是否已执行；
- 是否已有 preparing route；
- committed epoch 是否等于 baseEpoch；
- new epoch 是否严格等于 baseEpoch + 1；
- deadline 是否仍有效。

同 baseEpoch 的 100 个并发请求只有第一个创建 preparing route，其余读取同一个 assignment 或获得带当前快照的冲突。代码用并发测试固定这一不变式。

READY 必须匹配 session、routeEpoch、assignmentId、角色和 connectionEpoch。第一端 READY 只更新一个席位；第二端 READY 才原子提升为 committed。通知发送和状态写入可以短暂不原子，因为重连或超时后客户端通过 `SessionSnapshot` 读取权威事实。

### 7.3 Make-before-break

新路径处于 PREPARING 时不能关闭旧路径。只有 COMMIT 后：

1. 新 delegate 已具备双端连通性；
2. listener 已挂到新 delegate；
3. stable channel 原子提升 active epoch；
4. 才关闭旧 delegate。

若旧路径物理上已经中断，无法保证“无瞬时断流”，但逻辑上仍不能跳过 PREPARE/COMMIT，否则双端会独立快进到不同 relay。当前 READY 表示本端携带有效票据加入了指定 Relay 并建立 `SecureTransportChannel`，尚没有 READY 前的双端应用层 E2E 探测。

### 7.4 当前没有 Relay → P2P 热回切

进入 Relay 后，运行主链不会周期性重新打洞，也不会在同一会话中自动切回 P2P。Relay 状态收到的旧 ICE candidate 会被 `ClientConnectionManager` 忽略。此前存在的客户端单边回切会本地递增 `routeEpoch`，无法保证另一端同步，已经删除。

若以后增加该能力，P2P re-probe 必须成为服务端可持久化的 PREPARE 类型：双方保持旧 Relay，重新交换 candidate，各自建立并验证候选 P2P，双 READY 后由服务端提交新 epoch，失败则 ABORT 并继续使用 Relay。当前协议和代码没有实现这条链路。

## 8. 跨信令节点投递

设备 1 可能连 signaling-A，设备 2 连 signaling-B。A 不能访问 B 内存中的 Netty Channel，因此投递分两段：

```text
A: 权威状态先落 Redis
 → DeliveryEnvelope(messageId, targetDevice, targetEpoch, deadline, payload)
 → Redis Stream rc:v2:signal:inbox:{B}
B: inbox consumer
 → 检查 deadline
 → 检查本地 target connectionEpoch
 → messageId dedupe
 → 写本地 Channel
 → ACK/delete；失败进 DLQ 或由 snapshot 修复
```

这里追求“至少一次 + 幂等”，而不是宣传无法证明的“精确一次”。纯 Pub/Sub 在订阅方重启时会丢关键事件，所以只适合缓存失效等可丢通知，不承载邀请、COMMIT 和 END。

## 9. Relay V2 票据与席位

Ed25519 ticket claims 包含：issuer、keyId、jti、sessionId、routeEpoch、assignmentId、relayNodeId、pathType、role、deviceId、connectionEpoch 和时间窗。

安全效果：

- Relay-A 的票据不能拿到 Relay-B 使用；
- UDP 票据不能改走 WS；
- controller 票据不能占 agent 席位；
- epoch=7 票据不能加入 epoch=8；
- 两端使用不同 jti；
- 完全相同的 JOIN 重发可幂等成功，换 nonce/端点抢占会被 replay guard 拒绝。

V2 relay 包头显式包含 magic、version、type、session、epoch、path、role、seq 和 length。解析器先做版本、枚举和长度上限校验，未鉴权 DATA 直接丢弃，避免 UDP 放大。

## 10. 数据面 E2EE

### 10.1 密钥层级

```text
sessionMasterKey = HKDF(inviteEntropy,
                        sessionId || controllerId || agentId,
                        "rc-session-v2")

epochKey = HKDF(sessionMasterKey,
                "rc-data-v2" || routeEpoch || pathType)

send/receive key = HKDF(epochKey, "CONTROLLER_TO_AGENT" / "AGENT_TO_CONTROLLER")
```

epoch 和方向隔离意味着两端即使从 sequence=0 开始，也不会复用 `(key, nonce)`。路径切换后必须创建新的 `SecureTransportChannel`，不能把旧 key 和新 epoch 混用。

### 10.2 两层帧

```text
OuterTransportFrame（relay 可见）
  version | sessionId | routeEpoch | direction | streamId | packetSeq | ciphertext

EncryptedInnerFrame（AES-GCM 内）
  channel | type | flags | streamSeq | timestamp | payload
```

业务 channel 不能保留在 relay 可见的旧 DataFrame 头里。实现把 outer frame 统一经底层 CONTROL 通道发送，因此 relay 只能看到常量外层分类，不能区分屏幕、键鼠、文件和剪贴板。

nonce 是 96 bit：`epochLow32 | direction16 | stream16 | sequence32`。sequence 回绕前必须换 key/epoch。接收端先验 GCM tag，再推进 replay window，避免攻击者用伪造的超大序号提前“烧掉”合法接收窗口。

## 11. QoS、降级与背压

当前运行时会触发降级的证据按优先级排列：

1. transport close/reset；
2. PONG 静默超过 `gap EWMA + k·σ` 的动态门限（并受 floor/ceiling 截断）；
3. 滑动窗口丢包率持续超过阈值；
4. assignment 三次建连/JOIN 失败。

RTT 会由 PING/PONG 测量并进入本地 Micrometer 指标，但当前高 RTT 本身不触发降级；代码中的 RTT/卡顿常量尚未接入 `QosMonitor` 判定。QoS 触发 `RelayFailureReport` 时当前填入的 observed RTT/loss 仍为 0，因此 Redis 会学到一次失败，但不会从这次报告获得真实 RTT/丢包样本。

Redis Relay 画像不是降级触发器。它保存节点容量、CPU、带宽、direct-memory，以及按 `region:provider:path` 聚合的失败率/RTT/丢包 EWMA，仅在 `RelayManager.selectBest` 为下一次 assignment 排序时读取。当前服务端不会因为画像变差就主动扫描并迁移该节点的全部存量会话。

本地 `writeAndFlush` 成功只说明写入本机队列，不能证明对端可达。当前 Relay READY 也只证明本端已被 Relay 接纳，不等价于双端应用层探测已经成功。

视频/音频追求新鲜度，可在背压时丢旧帧；控制和文件要求可靠性，必须使用有界队列、ACK/offset 幂等，不能与视频共用无界 outbound buffer。

Relay 会话的活性依据 DATA/peer 查找刷新，不能只按首次 `joinedAt` 清理，否则长会话即使持续传输也会过期。V2 registry 同时以 epoch 和单调 packet sequence 阻止旧路径数据刷新新会话。

## 12. 故障语义

| 故障 | 系统行为 |
|---|---|
| 旧信令连接晚到 disconnect | connectionEpoch 不匹配，不能删除新 lease |
| 两端同时请求迁移 | CAS 只创建一个 preparing assignment |
| 旧 RelayAssignment 延迟到达 | routeEpoch/assignmentId 不匹配，客户端拒绝 |
| 只有一端 JOIN relay | 本端 seat accepted 并上报一个 READY；缺少另一端 READY，不会 COMMIT |
| COMMIT 通知丢失 | Redis committed route 仍是事实，snapshot 修复 |
| Redis 暂时不可用 | 保留旧数据面，暂停新会话/新路由，不本地 failover |
| relay 节点宕机 | 客户端从 close/静默/丢包感知并报告；coordinator 排除失败节点并创建下一 epoch |
| Nacos/发现层短暂不可用 | 订阅内存保留最后候选快照；已提交 route 不变，新选路失败则保留旧路径 |
| E2EE 帧被篡改/重放 | GCM 或 replay window 丢弃，不向业务分发 |
| 生产使用默认密钥/无 TLS | `ProductionSafetyValidator` 拒绝启动 |

## 13. 测试与证据

最近一次全量 `mvn clean test` 共运行 19 个测试且全部通过，覆盖：

- 四模块 Java 17 全量编译和 Protobuf 生成；
- Kwik 0.9.1 和 JCodec 0.2.5 实际 API；
- 连接 fencing；
- session 成员不可变、非成员拒绝、END 幂等；
- 100 个并发路由 prepare 请求只有一个 assignment；
- 双 READY 才 COMMIT，epoch 单调；
- Ed25519 ticket 篡改、错节点、角色抢占；
- Relay V2 JOIN payload、UDP/stream 角色席位与单调序号；
- Nacos metadata 到 Relay 能力映射、运行压力和失败 EWMA 评分；
- stable channel listener 跨 delegate 切换；
- nonce 隔离、AES-GCM 篡改和数据重放；
- SessionReducer 丢弃旧 epoch 事件；
- RelayClusterInvoker 辅助模型不做本地备选节点 failover（该模型当前未接入运行主链）。
- SSO 交接码不可推导 token，且只能消费一次。

单元测试不能替代外部系统验证。仓库提供 `deploy/docker-compose.cluster.yml`，可启动 MySQL、Redis、Nacos 3.2.3、双信令、双 Relay 与 Nginx 网关；正式发布仍需执行网络丢包/乱序、节点 kill、Redis 主从切换、TLS/mTLS、长时带宽和 direct-memory 测试。本次执行环境没有 Docker，因此只验证了 compose 文件、Java 构建以及 Signaling/Relay 进程启动，不把容器/Chaos 写成已通过。

## 14. 协议兼容边界

旧 Protobuf 字段仍保留是为了滚动升级期间能够解码历史消息，并不代表运行时允许旧 HMAC Relay 与 V2 会话混用。客户端注册声明 `min=max=2.0` 和能力集；服务端返回协商版本、connectionId 与 connectionEpoch。Relay 运行时只接受带 RCV2 magic 的 V2 包和 Ed25519 assignment ticket，旧 HMAC JOIN 不进入数据面。这避免了最危险的“半升级”：一端自主降级，另一端等待 COMMIT，最终形成永久半连接。

## 15. 代码阅读顺序

建议按以下顺序理解项目：

1. `common/src/main/proto/signal.proto`：先看真实长连接交换的事实；
2. `server-signaling/.../connection`：理解连接 fencing；
3. `server-signaling/.../session`：理解成员与终态；
4. `server-signaling/.../route`：理解 PREPARE/READY/COMMIT；
5. `server-signaling/.../messaging`：理解跨节点投递；
6. `common/.../crypto` 与 `common/.../codec`：理解票据、key、nonce 和帧；
7. `server-signaling/.../relay` 与 `server-relay/RelayNacosRegistrar`：理解发现和评分边界；
8. `server-relay/.../session`、`server-relay/.../tcp`：理解 UDP/stream 角色席位；
9. `client/app/ClientConnectionManager` 与 `client/transport`：理解当前运行主链如何重试、READY 和 COMMIT 而不仲裁；
10. `client/session` 与 `client/relay`：理解已测试但尚未接入运行主链的 reducer/actor 和 invoker 辅助模型。

最重要的设计结论只有一句：**候选可以最终一致，但一个会话的当前路径必须有唯一、可 fencing、可恢复的权威事实。**
